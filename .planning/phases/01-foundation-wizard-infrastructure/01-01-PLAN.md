---
phase: 01-foundation-wizard-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - src/index.css
  - src/main.tsx
  - src/App.tsx
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/toggle-group.tsx
  - src/components/ui/radio-group.tsx
  - components.json
  - index.html
autonomous: true

must_haves:
  truths:
    - "Running npm run dev starts a development server without errors"
    - "Tailwind CSS utilities render correctly in the browser"
    - "shadcn/ui components are importable and render"
    - "TypeScript compilation succeeds with no errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "vite"
    - path: "vite.config.ts"
      provides: "Vite configuration with Tailwind plugin"
      contains: "@tailwindcss/vite"
    - path: "src/index.css"
      provides: "Tailwind 4 CSS entry"
      contains: "@import \"tailwindcss\""
    - path: "src/lib/utils.ts"
      provides: "shadcn/ui cn() utility"
      exports: ["cn"]
    - path: "src/components/ui/button.tsx"
      provides: "Button component"
      exports: ["Button"]
  key_links:
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugin import"
      pattern: "import tailwindcss"
    - from: "src/main.tsx"
      to: "src/index.css"
      via: "CSS import"
      pattern: "import.*index\\.css"
---

<objective>
Scaffold the project foundation with Vite 7.x, React 19, TypeScript, Tailwind CSS 4, and shadcn/ui.

Purpose: Establish the technical foundation that all subsequent plans build upon. Without this scaffolding, no UI or state management work can proceed.

Output: A working Vite + React project with Tailwind 4 styling and shadcn/ui components ready for use.
</objective>

<execution_context>
@/Users/ryan.lucht/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryan.lucht/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-wizard-infrastructure/01-RESEARCH.md

Tailwind 4 uses NEW syntax:
- Use `@import "tailwindcss"` NOT `@tailwind base/components/utilities`
- Use `@tailwindcss/vite` plugin, NOT PostCSS
- Theme customization uses CSS `@theme` block
- Some utilities renamed: `shadow-sm` -> `shadow-xs`, `rounded-sm` -> `rounded-xs`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React 19 + TypeScript project</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tsconfig.app.json
    tsconfig.node.json
    src/main.tsx
    src/App.tsx
    index.html
  </files>
  <action>
    1. Initialize the project using: `npm create vite@latest . -- --template react-swc-ts`
       - Use `.` to create in current directory (project root already exists)
       - If prompted about existing files, proceed (only CLAUDE.md, LICENSE, SPEC.md exist)

    2. Install core dependencies:
       ```bash
       npm install zustand react-intersection-observer
       npm install tailwindcss @tailwindcss/vite
       npm install -D @types/node vitest @testing-library/react @testing-library/jest-dom jsdom
       ```

    3. Update vite.config.ts to add Tailwind plugin and path aliases:
       ```typescript
       import path from 'path';
       import tailwindcss from '@tailwindcss/vite';
       import react from '@vitejs/plugin-react-swc';
       import { defineConfig } from 'vite';

       export default defineConfig({
         plugins: [react(), tailwindcss()],
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
         test: {
           globals: true,
           environment: 'jsdom',
           setupFiles: './src/test/setup.ts',
         },
       });
       ```

    4. Update tsconfig.json to add path aliases (add to compilerOptions):
       ```json
       "baseUrl": ".",
       "paths": {
         "@/*": ["./src/*"]
       }
       ```

    5. Create src/test/setup.ts with:
       ```typescript
       import '@testing-library/jest-dom';
       ```

    6. Update src/App.tsx to a minimal placeholder:
       ```tsx
       function App() {
         return (
           <div className="min-h-screen bg-background text-foreground p-8">
             <h1 className="text-2xl font-bold">Should I Test That?</h1>
             <p className="text-muted-foreground mt-2">Calculator loading...</p>
           </div>
         );
       }
       export default App;
       ```
  </action>
  <verify>
    Run `npm run dev` - should start without errors.
    Run `npm run build` - should compile without TypeScript errors.
  </verify>
  <done>
    Vite dev server starts, TypeScript compiles, path aliases resolve.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind 4 and install shadcn/ui components</name>
  <files>
    src/index.css
    src/lib/utils.ts
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/components/ui/input.tsx
    src/components/ui/label.tsx
    src/components/ui/toggle-group.tsx
    src/components/ui/radio-group.tsx
    components.json
  </files>
  <action>
    1. Update src/index.css for Tailwind 4:
       ```css
       @import "tailwindcss";

       /* Datadog-inspired theme - clean, modern, high-contrast */
       @theme {
         --color-background: oklch(0.99 0.002 250);
         --color-foreground: oklch(0.15 0.02 250);
         --color-card: oklch(1 0 0);
         --color-card-foreground: oklch(0.15 0.02 250);
         --color-primary: oklch(0.55 0.2 260);
         --color-primary-foreground: oklch(0.98 0.01 260);
         --color-secondary: oklch(0.95 0.01 250);
         --color-secondary-foreground: oklch(0.2 0.02 250);
         --color-muted: oklch(0.95 0.01 250);
         --color-muted-foreground: oklch(0.5 0.02 250);
         --color-accent: oklch(0.95 0.01 250);
         --color-accent-foreground: oklch(0.2 0.02 250);
         --color-destructive: oklch(0.55 0.2 25);
         --color-destructive-foreground: oklch(0.98 0.01 25);
         --color-border: oklch(0.9 0.01 250);
         --color-input: oklch(0.9 0.01 250);
         --color-ring: oklch(0.55 0.2 260);
         --radius: 0.5rem;
       }

       /* Base styles */
       * {
         border-color: var(--color-border);
       }

       body {
         background-color: var(--color-background);
         color: var(--color-foreground);
         font-family: system-ui, -apple-system, sans-serif;
       }
       ```

    2. Initialize shadcn/ui:
       ```bash
       npx shadcn@latest init
       ```
       When prompted:
       - Style: Default
       - Base color: Slate
       - CSS variables: Yes
       - CSS file location: src/index.css (it will merge with existing)
       - Tailwind config: Skip (we use Tailwind 4 CSS config)
       - Components alias: @/components
       - Utils alias: @/lib/utils
       - Use React Server Components: No

    3. Install required shadcn/ui components:
       ```bash
       npx shadcn@latest add button card input label toggle-group radio-group
       ```

    4. Verify src/lib/utils.ts exists with cn() function:
       ```typescript
       import { clsx, type ClassValue } from "clsx";
       import { twMerge } from "tailwind-merge";

       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs));
       }
       ```

    5. Update src/App.tsx to use shadcn components and verify styling:
       ```tsx
       import { Button } from '@/components/ui/button';
       import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

       function App() {
         return (
           <div className="min-h-screen bg-background text-foreground p-8">
             <h1 className="text-2xl font-bold mb-6">Should I Test That?</h1>

             <Card className="max-w-md">
               <CardHeader>
                 <CardTitle>Project Setup Complete</CardTitle>
                 <CardDescription>
                   Vite + React 19 + TypeScript + Tailwind 4 + shadcn/ui
                 </CardDescription>
               </CardHeader>
               <CardContent className="space-y-4">
                 <p className="text-muted-foreground">
                   The foundation is ready. Next: design system and wizard navigation.
                 </p>
                 <Button>Get Started</Button>
               </CardContent>
             </Card>
           </div>
         );
       }
       export default App;
       ```
  </action>
  <verify>
    Run `npm run dev` and open in browser:
    - Card component renders with proper styling
    - Button shows primary color on hover
    - Text uses proper foreground/muted colors
    - No console errors

    Run `npm run build` - should succeed with no errors.
  </verify>
  <done>
    shadcn/ui components (Button, Card, Input, Label, ToggleGroup, RadioGroup) are installed and render with Datadog-inspired theme. Tailwind 4 CSS variables work correctly.
  </done>
</task>

</tasks>

<verification>
After completing both tasks:
1. `npm run dev` starts without errors
2. `npm run build` compiles without errors
3. Browser shows styled Card with Button
4. Path aliases (@/) resolve correctly
5. All shadcn components import without errors

Test command sequence:
```bash
npm run build && npm run dev
```
</verification>

<success_criteria>
- Vite dev server runs on localhost
- TypeScript strict mode compiles without errors
- Tailwind 4 utilities apply correctly (visible in browser)
- shadcn/ui components render with proper styling
- Path alias @/ resolves to src/
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-wizard-infrastructure/01-01-SUMMARY.md`
</output>
