---
phase: 25.1-results-card-improvements
verified: 2026-04-10T12:00:00Z
status: human_needed
score: 12/12 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 10/12
  gaps_closed:
    - "Hook passes directional probabilities through to UI consumers — useEVSICalculations.test.ts now uses setInput() exclusively; 18 tests pass"
    - "Statistical Interpretation includes directional phrasing — directionSentence variable (guardrail/confidence builder) wired into WaterfallBlock step 1; plan-vs-implementation label mismatch confirmed resolved"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Full results page layout review"
    expected: "Verdict > Warnings > ValueBreakdown > SupportingCards > Waterfall (Plain-English explanation panel) > Export > FAQAccordion matches the PM-approved layout from Plan 04 Round 1 feedback"
    why_human: "Layout order is visual and can only be confirmed by running the app"
  - test: "WaterfallBlock step 1 tie-break copy"
    expected: "When prior mean equals shipping threshold exactly, step 1 reads 'right at the boundary' and explains tie-break convention; defaults to ship; notes this doesn't affect test value"
    why_human: "Requires specific input values (symmetric prior with any-positive threshold) to trigger the isTie path; visual confirmation needed"
  - test: "FAQ accordion content accuracy for non-statisticians"
    expected: "All four FAQ answers read naturally; dynamic values (traffic, valuePerConversion, timingCost, testValue) are correctly interpolated and make business sense"
    why_human: "Content quality, tone, and accuracy for non-statistician readers requires human judgment"
  - test: "Decision impact card — dominant direction rendering"
    expected: "Only the dominant directional row appears (P(Keeps you from shipping) when defaultDecision='ship'; P(Pushes you to ship) when defaultDecision='dont-ship'); near-zero direction hidden"
    why_human: "Conditional rendering based on real engine output requires running the app to confirm"
---

# Phase 25.1: Results Card Improvements Verification Report

**Phase Goal:** Results page is understandable to non-statisticians: plain-English waterfall explains why the calculator produced its value, directional decision impact replaces abstract P(Decision Change), card labels use plain language, and FAQ accordions answer follow-up questions on demand
**Verified:** 2026-04-10
**Status:** human_needed (all automated checks pass; 4 items require human confirmation)
**Re-verification:** Yes — after gap closure (hook test mocks fixed; directionSentence integration confirmed)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | EVSIResults includes pStopsShip and pConvincesShip fields | VERIFIED | `types.ts` lines 220, 227: `pStopsShip?: number` and `pConvincesShip?: number` in EVSIResults |
| 2 | NetValueResults includes pStopsShip and pConvincesShip fields | VERIFIED | `types.ts` lines 317, 324: same optional fields in NetValueResults |
| 3 | Monte Carlo loops track directional decision change counts | VERIFIED | `evsi.ts` lines 526-527, 591-593, 667-668: `stopsShipCount`/`convincesShipCount` counters, increments, and probability computation; identical pattern in `net-value.ts` lines 343-344, 401-403, 477-478 |
| 4 | Normal fast-path derives directional probabilities from defaultDecision | VERIFIED | `evsi.ts` lines 866-870: `pStopsShip = defaultDecision === 'ship' ? probabilityTestChangesDecision : 0` (and symmetric for pConvincesShip) |
| 5 | Hook passes directional probabilities through to UI consumers | VERIFIED | `useEVSICalculations.test.ts` uses `setInput()` exclusively (no stale setSharedInput/setAdvancedInput/setMode calls); 18 tests pass including directional probability identity test at line 262 |
| 6 | A six-step plain-English waterfall narrative is visible by default below the verdict card | VERIFIED | `WaterfallBlock.tsx` renders `<section aria-label="Plain English explanation">` styled as blue info panel with `<h4>Plain English explanation</h4>` and `<ol>` with 6 `<li>` steps; wired unconditionally in `AdvancedResultsSection.tsx` line 248 inside `results &&` guard |
| 7 | SupportingCard grid shows 'Shipping rule' instead of 'Threshold' | VERIFIED | `AdvancedResultsSection.tsx` line 183: `title="Shipping rule"`; no `title="Threshold"` present in file |
| 8 | SupportingCard grid shows 'Decision impact' with directional rows instead of single P(Decision Change) | VERIFIED | `AdvancedResultsSection.tsx` line 200: `title="Decision impact"` with conditional `P(Keeps you from shipping)` / `P(Pushes you to ship)` rows using `pStopsShip` and `pConvincesShip` |
| 9 | Statistical Interpretation includes directional phrasing (per PM-approved revision) | VERIFIED | `AdvancedResultsSection.tsx` lines 235-244: `directionSentence` variable computed from `pStopsShip`/`pConvincesShip`; produces guardrail / confidence-builder / both-directions copy; passed to WaterfallBlock as `directionSentence` prop at line 258 |
| 10 | Four FAQ accordion items are rendered collapsed by default | VERIFIED | `FAQAccordion.tsx`: four independent `useState(false)` booleans (open1-4); `AccordionItem` renders `aria-expanded={isOpen}` where isOpen starts false |
| 11 | Each accordion expands independently to show dynamic answer content | VERIFIED | No mutual-exclusion logic; toggle functions are independent `() => setOpenN(!openN)`; dynamic values (traffic, valuePerConversion, priorLow/High, pDecisionChange, testValue, timingCost) interpolated via props |
| 12 | EVSI Intuition block is replaced by FAQ accordions | VERIFIED | No "EVSI Intuition" or "How to interpret" text found in `AdvancedResultsSection.tsx`; `FAQAccordion` imported at line 24 and rendered at line 291 with all required props |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/calculations/types.ts` | pStopsShip/pConvincesShip on EVSIResults and NetValueResults | VERIFIED | Both interfaces contain optional directional fields with JSDoc comments at lines 220-227 and 317-324 |
| `src/lib/calculations/evsi.ts` | Directional counters in MC loop and fast-path derivation | VERIFIED | `stopsShipCount`, `convincesShipCount` in MC; fast-path derivation at lines 866-870 |
| `src/lib/calculations/net-value.ts` | Directional counters in MC loop | VERIFIED | `stopsShipCount`, `convincesShipCount` present with identical MC pattern |
| `src/hooks/useEVSICalculations.ts` | Hook plumbing for directional probabilities | VERIFIED | Fields flow via EVSIResults type automatically; hook tests confirm access to `evsi.pStopsShip` and `evsi.pConvincesShip` |
| `src/hooks/useEVSICalculations.test.ts` | Hook tests using correct store API | VERIFIED (was STUB) | All calls use `setInput()` per flat WizardStore API; 18 tests pass including directional probability test |
| `src/components/results/WaterfallBlock.tsx` | Six-step waterfall narrative as blue info panel | VERIFIED | 149 lines; blue info panel (`bg-blue-50 border border-blue-200`); exports `WaterfallBlock`; accepts `directionSentence` prop merged into step 1; wired in AdvancedResultsSection |
| `src/components/results/WaterfallBlock.test.tsx` | Tests for waterfall rendering | VERIFIED | Tests pass (included in the 60-test results suite) |
| `src/components/results/SupportingCard.tsx` | children prop extension | VERIFIED | `children?: ReactNode` prop present; conditional rendering logic correct |
| `src/components/results/AdvancedResultsSection.tsx` | Updated layout with all new components | VERIFIED | WaterfallBlock, FAQAccordion imported and wired; "Shipping rule" and "Decision impact" confirmed; `directionSentence` computed and passed |
| `src/components/results/FAQAccordion.tsx` | Four-item FAQ accordion with dynamic content | VERIFIED | 212 lines; all four question labels present; dynamic value interpolation confirmed |
| `src/components/results/FAQAccordion.test.tsx` | Tests for accordion expand/collapse | VERIFIED | 11 tests, all passing |
| `src/components/results/index.ts` | Exports for WaterfallBlock and FAQAccordion | VERIFIED | Lines 5-6: `export { WaterfallBlock }` and `export { FAQAccordion }` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/lib/calculations/evsi.ts` | `src/lib/calculations/types.ts` | EVSIResults type with pStopsShip/pConvincesShip | WIRED | Return statements include `pStopsShip, pConvincesShip` in all code paths |
| `src/hooks/useEVSICalculations.ts` | `src/lib/calculations/types.ts` | EVSIResults flowing through hook return | WIRED | EVSICalculationResults.evsi typed as EVSIResults; directional fields flow automatically |
| `src/components/results/WaterfallBlock.tsx` | `src/lib/formatting` | formatSmartCurrency, formatProbabilityPercent, formatPercentage | WIRED | All three formatters imported and used in step content |
| `src/components/results/AdvancedResultsSection.tsx` | `src/components/results/WaterfallBlock.tsx` | import and render with directionSentence prop | WIRED | Line 23: import; line 248: `<WaterfallBlock ...>` with all required props including `directionSentence` and `isTie` |
| `src/components/results/FAQAccordion.tsx` | `src/lib/formatting` | formatSmartCurrency, formatProbabilityPercent, formatPercentage, formatNumber | WIRED | All formatters imported and used in accordion answer content |
| `src/components/results/AdvancedResultsSection.tsx` | `src/components/results/FAQAccordion.tsx` | import and render in layout | WIRED | Line 24: import; line 291: `<FAQAccordion ...>` with all required props |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `WaterfallBlock.tsx` | testValue, netValue, timingCost | `results.evsi.evsiDollars`, `results.netValueDollars` | Yes — from EVSI calculation engine | FLOWING |
| `WaterfallBlock.tsx` | defaultDecision | `results.evsi.defaultDecision` | Yes — computed by engine | FLOWING |
| `WaterfallBlock.tsx` | priorLow, priorHigh | `inputs.priorIntervalLow`, `inputs.priorIntervalHigh` with defaults | Yes — from wizard store | FLOWING |
| `WaterfallBlock.tsx` | directionSentence | Derived in AdvancedResultsSection from `results.evsi.pStopsShip`/`pConvincesShip` and `defaultDecision` | Yes — computed from engine output | FLOWING |
| `FAQAccordion.tsx` | traffic | `inputs.dailyTraffic ?? 0` | Yes — from wizard store | FLOWING |
| `FAQAccordion.tsx` | valuePerConversion | `inputs.valuePerConversion ?? 0` | Yes — from wizard store | FLOWING |
| `FAQAccordion.tsx` | timingCost | `results.evsi.evsiDollars - results.netValueDollars` | Yes — derived from engine output | FLOWING |
| `AdvancedResultsSection.tsx` | pStopsShip, pConvincesShip | `results.evsi.pStopsShip ?? 0`, `results.evsi.pConvincesShip ?? 0` | Yes — computed by MC engine | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| WaterfallBlock tests pass | `npx vitest run src/components/results/WaterfallBlock.test.tsx` | Included in results suite 60 PASS, 0 FAIL | PASS |
| FAQAccordion tests pass | `npx vitest run src/components/results/FAQAccordion.test.tsx` | Included in results suite 60 PASS, 0 FAIL | PASS |
| All results component tests | `npx vitest run src/components/results/` | 60 PASS, 0 FAIL | PASS |
| Hook tests (Gap 1 re-check) | `npx vitest run src/hooks/useEVSICalculations.test.ts` | 18 PASS, 0 FAIL | PASS |
| Engine directional probability tests | `npx vitest run src/lib/calculations/evsi.test.ts src/lib/calculations/net-value.test.ts` | Included in 455 pass total | PASS |
| Full test suite | `npx vitest run` | 455 PASS, 2 FAIL | PASS (2 failures are pre-existing ENG-03 feasibility.test.ts, unrelated to phase 25.1) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| RCI-01 | 25.1-01-PLAN.md | Directional probability fields (pStopsShip/pConvincesShip) added to calculation engine | SATISFIED | types.ts contains both optional fields; evsi.ts and net-value.ts have directional MC counters and fast-path derivation; 18 hook tests confirm pass-through |
| RCI-02 | 25.1-02-PLAN.md, 25.1-04-PLAN.md | Six-step "Why this result?" waterfall visible by default | SATISFIED | WaterfallBlock.tsx renders 6-step narrative as blue info panel; wired unconditionally in AdvancedResultsSection inside results guard |
| RCI-03 | 25.1-02-PLAN.md, 25.1-04-PLAN.md | "Threshold" card renamed to "Shipping rule" | SATISFIED | AdvancedResultsSection.tsx line 183: `title="Shipping rule"`; no "Threshold" card title present |
| RCI-04 | 25.1-02-PLAN.md, 25.1-04-PLAN.md | Single P(Decision Change) replaced with directional rows | SATISFIED | "Decision impact" card with conditional P(Keeps you from shipping)/P(Pushes you to ship) rows; PM-approved copy revision from Round 1 |
| RCI-05 | 25.1-02-PLAN.md, 25.1-04-PLAN.md | Statistical Interpretation callout uses directional phrasing | SATISFIED | `directionSentence` variable computed from directional probabilities (guardrail/confidence builder/both); merged into WaterfallBlock step 1 as PM-approved plain-English interpretation panel |
| RCI-06 | 25.1-03-PLAN.md, 25.1-04-PLAN.md | Four FAQ accordion explainers collapsed by default, expand independently | SATISFIED | FAQAccordion.tsx: 4 independent `useState(false)` booleans; all labels present; `aria-expanded`, `role="button"`, `tabIndex={0}`, `onKeyDown` keyboard support |

**Orphaned Requirements Check:** REQUIREMENTS.md does not contain RCI-01 through RCI-06 entries — these requirements were added for phase 25.1 and are defined solely in ROADMAP.md's Coverage Validation table (lines 293-298). All six are mapped to phase 25.1. No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/components/results/AdvancedResultsSection.test.tsx` | ~57 | Mock references `cod: sampleCodResults` field (pre-existing) | Warning | Tests pass (60/60); TypeScript structural typing permits extra fields; no functional impact |

No blockers found. The previous blocker (stale store API calls in hook tests) is resolved.

### Human Verification Required

#### 1. Full Results Page Layout Review

**Test:** Run `npm run dev`, navigate to calculator, fill all form sections to produce results, and observe the full layout order
**Expected:** Verdict > Warnings > ValueBreakdown > SupportingCards > Waterfall (blue "Plain English explanation" panel) > Export > FAQAccordion — this is the PM-approved layout from Plan 04 Round 1 feedback
**Why human:** Layout order is visual and cannot be verified programmatically without rendering the app. Note: the layout comment in AdvancedResultsSection.tsx line 14 shows the intended order.

#### 2. WaterfallBlock Step 1 — Tie-Break Copy

**Test:** Set prior interval to [-5%, +5%] (symmetric, so mean = 0.0) with "any-positive" threshold; observe Step 1 text
**Expected:** Step 1 reads "Your expected effect (+0.0%) is right at the boundary of your shipping rule (any positive impact). At this point, shipping and not shipping are equally good — the calculator defaults to ship, and this choice doesn't affect the test's value."
**Why human:** Requires exact symmetric input values to trigger the `isTie` path; visual confirmation needed

#### 3. FAQ Accordion Content Accuracy for Non-Statisticians

**Test:** With a complete set of inputs producing results, click each of the four FAQ accordion triggers and read the expanded answers
**Expected:** Each answer reads naturally; dynamic values (traffic count, conversion value, timing cost, test value) are correctly interpolated and make business sense; accordions open and close independently; all start collapsed
**Why human:** Content quality, tone, and accuracy for non-statistician readers requires human judgment

#### 4. Decision Impact Card — Dominant Direction Rendering

**Test:** With `defaultDecision='ship'` results, verify only "P(Keeps you from shipping)" row is visible; with `defaultDecision='dont-ship'` results, verify only "P(Pushes you to ship)" row is visible
**Expected:** Exactly one directional row visible at a time; near-zero probability direction is hidden; total probability still shown in footer row
**Why human:** Requires real calculation results to observe which direction dominates and confirm conditional row rendering

### Gaps Summary

No gaps remain. Both gaps from initial verification are closed:

**Gap 1 (CLOSED):** `useEVSICalculations.test.ts` now uses `setInput()` exclusively (the flat WizardStore API). All 18 tests pass. The stale `setSharedInput`/`setAdvancedInput`/`setMode` calls that caused 20 test failures are gone.

**Gap 2 (CLOSED — nominal):** The `mainDecisionMechanism` variable name from Plan 02's must_have pattern was replaced by `directionSentence` per PM feedback in Plan 04. This is a PM-approved implementation evolution, not a missing feature. The observable goal (plain-English directional interpretation of the statistical result) is fully achieved via the `directionSentence` logic in `AdvancedResultsSection.tsx` (lines 235-244) and its integration into WaterfallBlock step 1.

**Pre-existing failures (unchanged):** 2 failures in `src/lib/calculations/feasibility.test.ts` (ENG-03 from phases 20-21) were present before this phase and are out of scope.

---

_Verified: 2026-04-10T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes (previous status: gaps_found, previous score: 10/12)_
