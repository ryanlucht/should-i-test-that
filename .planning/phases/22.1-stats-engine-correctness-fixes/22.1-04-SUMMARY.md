---
phase: 22.1-stats-engine-correctness-fixes
plan: 04
subsystem: calculations
tags: [evsi, worker, truncation, normal-prior, dead-code]

# Dependency graph
requires:
  - phase: 22.1-01
    provides: feasibility.ts with computeInfeasibleTailMass and TRUNCATION_THRESHOLD
provides:
  - Worker truncation gate matching hook behavior for Normal priors
  - Dead truncated-normal.ts module removed
affects: [22.1-05, engine-accuracy]

# Tech tracking
tech-stack:
  added: []
  patterns: [worker-hook truncation parity via shared TRUNCATION_THRESHOLD constant]

key-files:
  created: []
  modified:
    - src/lib/workers/evsi.worker.ts
    - src/lib/calculations/evsi.test.ts
  deleted:
    - src/lib/calculations/truncated-normal.ts
    - src/lib/calculations/truncated-normal.test.ts

key-decisions:
  - "Worker uses <= (not <) for truncation gate boundary to match hook exactly"
  - "truncated-normal.ts confirmed dead (only imported by own test) and deleted"

patterns-established:
  - "Worker-hook parity: worker imports same feasibility functions as hook for consistent routing"

requirements-completed: [AUDIT-P2, AUDIT-P8a]

# Metrics
duration: 4min
completed: 2026-03-25
---

# Phase 22.1 Plan 04: Worker Truncation Routing + Dead Code Removal Summary

**Worker truncation gate added using computeInfeasibleTailMass/TRUNCATION_THRESHOLD from feasibility.ts; dead truncated-normal.ts module deleted**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-25T20:20:37Z
- **Completed:** 2026-03-25T20:24:50Z
- **Tasks:** 2
- **Files modified:** 2 modified, 2 deleted

## Accomplishments
- Worker computeEVSI now checks infeasible tail mass before taking Normal fast path, matching the hook's truncation gate exactly
- Boundary condition uses <= operator (infeasibleMass === TRUNCATION_THRESHOLD takes fast path), verified by test
- Dead truncated-normal.ts module (241 lines) and its test file (330 lines) deleted -- only imported by its own test
- 4 new routing tests added covering truncation-sensitive, low-truncation, MC validity, and boundary conditions
- Full test suite (452 tests) passes, TypeScript clean

## Task Commits

Each task was committed atomically:

1. **Task 1: Add truncation gate to worker and write routing test** - `dd8860f` (feat)
2. **Task 2: Delete dead truncated-normal.ts and its test file** - `b4223ea` (chore)

## Files Created/Modified
- `src/lib/workers/evsi.worker.ts` - Added truncation gate: imports computeInfeasibleTailMass + TRUNCATION_THRESHOLD, checks before Normal fast path
- `src/lib/calculations/evsi.test.ts` - Added 4 worker truncation routing tests
- `src/lib/calculations/truncated-normal.ts` - DELETED (dead code, one-sided truncation functions unused)
- `src/lib/calculations/truncated-normal.test.ts` - DELETED (test file for dead module)

## Decisions Made
- Worker uses `<=` (not `<`) for truncation gate to match hook behavior at boundary exactly
- Confirmed truncatedNormalMeanTwoSided in evsi.ts is a separate function from the deleted one-sided functions in truncated-normal.ts
- Verified module was dead via grep: only its own test file imported it

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Worktree was behind main (missing 22.1-01 changes including feasibility.ts). Fast-forward merged main to get the dependency before proceeding.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Worker now respects truncation gate, consistent with hook routing
- Dead code removed, reducing codebase by ~571 lines
- Ready for plan 05 (final verification/integration)

## Self-Check: PASSED

All artifacts verified: SUMMARY.md exists, worker modified, tests added, dead files deleted, both commits found (dd8860f, b4223ea).

---
*Phase: 22.1-stats-engine-correctness-fixes*
*Completed: 2026-03-25*
