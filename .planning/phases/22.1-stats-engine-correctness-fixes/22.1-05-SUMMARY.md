---
phase: 22.1-stats-engine-correctness-fixes
plan: 05
subsystem: calculations
tags: [bayesian, truncation, normal-conjugate, posterior-mean, warnings, evsi]

# Dependency graph
requires:
  - phase: 22.1-04
    provides: "Centralized buildPriorFromInputs, feasibility module with TRUNCATION_THRESHOLD"
provides:
  - "computePosteriorMean with truncated-Normal posterior for Normal priors under truncation"
  - "Merged EVSI + net-value warnings plumbed to UI"
affects: [22.1-03, results-display, export]

# Tech tracking
tech-stack:
  added: []
  patterns: ["truncated-Normal posterior for bounded parameters", "warning dedup by code"]

key-files:
  created: []
  modified:
    - "src/lib/calculations/evsi.ts"
    - "src/lib/calculations/evsi.test.ts"
    - "src/hooks/useEVSICalculations.ts"
    - "src/components/results/AdvancedResultsSection.tsx"

key-decisions:
  - "Test 1 uses mu_L=0.05 (not 0) with L_hat=0.10 to achieve >5% truncation effect, since posterior near L_max produces larger truncation shift"
  - "Warning dedup uses CalculationWarning.code as unique key (sufficient for existing warning taxonomy)"

patterns-established:
  - "Truncation-aware posterior: check computeInfeasibleTailMass before applying truncatedNormalMeanTwoSided to posterior"
  - "Warning merging: collect from all calculation sources, dedup by code, expose as top-level array"

requirements-completed: [AUDIT-P3, AUDIT-P8b]

# Metrics
duration: 5min
completed: 2026-03-25
---

# Phase 22.1 Plan 05: Truncated-Normal Posterior Mean + Warning Plumbing Summary

**Truncated-Normal posterior mean for Normal priors under feasibility truncation, with EVSI + net-value warnings merged and plumbed to UI**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-25T20:28:32Z
- **Completed:** 2026-03-25T20:34:05Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- computePosteriorMean now applies truncated-Normal formula when prior has material infeasible tail mass, fixing biased posterior estimates for high-CR0 cases (audit Priority 3)
- posteriorVariance = 1/(1/sigma_prior^2 + 1/SE^2) verified against known analytic case via indirect test
- Net-value warnings (previously silently dropped) now merge with EVSI warnings and render in the UI warning banner (audit Priority 8b)
- 4 new tests covering truncation divergence, untruncated regime, bounds compliance, and formula verification

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement truncated-Normal posterior mean for Normal priors under truncation** - `a729ad1` (feat)
2. **Task 2: Plumb net-value warnings to the UI** - `fb006fd` (feat)

_Note: Task 1 was TDD -- tests written first (RED), then implementation (GREEN). No refactor phase needed._

## Files Created/Modified
- `src/lib/calculations/evsi.ts` - computePosteriorMean Normal branch now detects truncation via computeInfeasibleTailMass and applies truncatedNormalMeanTwoSided to posterior
- `src/lib/calculations/evsi.test.ts` - 4 new tests in "Truncated-Normal posterior mean (audit P3)" describe block
- `src/hooks/useEVSICalculations.ts` - EVSICalculationResults gains warnings: CalculationWarning[]; finalResults merges EVSI + net-value warnings with code-based dedup
- `src/components/results/AdvancedResultsSection.tsx` - Warning rendering uses results.warnings (merged) instead of results.evsi.warnings (EVSI-only)

## Decisions Made
- Test 1 parameters adjusted from plan suggestion (mu_L=0 to mu_L=0.05, L_hat=0.05 to L_hat=0.10) because the original parameters only produced ~2% truncation effect. With posterior mean closer to L_max (0.53 sigma away), the truncation shift is ~14.8%, clearly exceeding the 5% threshold.
- Warning dedup uses only CalculationWarning.code as the unique key. Since each warning type has a distinct code (rare_events, high_rejection, low_acceptance, etc.), this is sufficient and avoids over-collapsing different instances.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Adjusted Test 1 parameters for >5% truncation divergence**
- **Found during:** Task 1 (TDD RED/GREEN)
- **Issue:** Plan-specified parameters (mu_L=0, L_hat=0.05) produced only ~2% relative difference between truncated and untruncated posterior means, failing the >5% assertion
- **Fix:** Changed to mu_L=0.05, L_hat=0.10 which places the posterior mean 0.53 sigma from L_max, producing ~14.8% truncation effect
- **Files modified:** src/lib/calculations/evsi.test.ts
- **Verification:** Test passes with 14.8% > 5% margin
- **Committed in:** a729ad1 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug in test parameters)
**Impact on plan:** Minor parameter adjustment to test case. No scope change. The truncation logic itself is exactly as planned.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 05 is the final plan in phase 22.1
- All audit priorities addressed across plans 01-05: Student-t scale (P1), worker truncation routing (P2), truncated posterior (P3), centralized prior (P4), threshold formatting (P5), traffic semantics (P6), legacy CoD removal (P7), warning plumbing (P8b)
- Phase 22.1 stats engine correctness fixes complete

## Self-Check: PASSED

All files exist, all commits verified.

---
*Phase: 22.1-stats-engine-correctness-fixes*
*Completed: 2026-03-25*
