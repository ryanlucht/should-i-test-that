---
phase: 22.1-stats-engine-correctness-fixes
verified: 2026-03-25T21:00:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
---

# Phase 22.1: Stats Engine Correctness Fixes — Verification Report

**Phase Goal:** Fix critical stats engine correctness issues identified in audit — Student-t double-division, threshold formatting, worker truncation routing, truncated-Normal posterior, legacy CoD removal, and warning plumbing.
**Verified:** 2026-03-25
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| #  | Truth                                                                                                             | Status     | Evidence                                                                                 |
|----|-------------------------------------------------------------------------------------------------------------------|------------|------------------------------------------------------------------------------------------|
| 1  | Student-t prior with [-8.22, 8.22] and df=5 produces sigma_L ~ 0.04079 (not 0.0004079)                          | VERIFIED   | `buildPriorFromInputs` test asserts `toBeCloseTo(0.04079, 3)`; `computeStudentTPriorScale` uses `jStat.studentt.inv(0.95, df)` |
| 2  | Worker does not use Normal fast path when infeasible tail mass exceeds TRUNCATION_THRESHOLD                       | VERIFIED   | `evsi.worker.ts` line 58: `if (infeasibleMass <= TRUNCATION_THRESHOLD)` — MC path taken when mass exceeds threshold |
| 3  | computePosteriorMean returns truncated-Normal posterior mean when truncation is active                            | VERIFIED   | `evsi.ts` line 376: `if (infeasibleMass > TRUNCATION_THRESHOLD)` calls `truncatedNormalMeanTwoSided` |
| 4  | All prior-construction sites (hook, form preview, results) use single buildPriorFromInputs helper                | VERIFIED   | `useEVSICalculations.ts:138`, `UncertaintyPriorForm.tsx:615`, `AdvancedResultsSection.tsx:45` all call `buildPriorFromInputs` |
| 5  | Dollar thresholds display as currency, not as percentage                                                          | VERIFIED   | `formatThreshold()` exists in `formatting.ts`; test asserts `'+$10,000'`; wired to `AdvancedResultsSection`, `ExportCard`, `ExportButton` |
| 6  | dailyTraffic label/tooltip clarifies total traffic before eligibility filtering                                   | VERIFIED   | `ExperimentDesignForm.tsx:190`: `label="Total daily visitors"`, tooltip contains "before experiment eligibility filtering" |
| 7  | Legacy calculateCostOfDelay and CoDResults removed from codebase                                                  | VERIFIED   | Grep for `calculateCostOfDelay`, `CoDResults`, `cod=\{` returns zero code matches (only comments in hook) |
| 8  | Net-value warnings reach the UI warning renderer                                                                  | VERIFIED   | `EVSICalculationResults` has `warnings: CalculationWarning[]`; hook merges `netValueResults.warnings`; `AdvancedResultsSection.tsx:96` renders `results.warnings` |

**Score: 8/8 truths verified**

---

### Required Artifacts

| Artifact                                        | Expected                                                         | Status     | Details                                                                      |
|-------------------------------------------------|------------------------------------------------------------------|------------|------------------------------------------------------------------------------|
| `src/lib/prior.ts`                              | `buildPriorFromInputs()` centralized helper                      | VERIFIED   | Exported at line 206; uses `computeStudentTPriorScale` with `jStat.studentt.inv` |
| `src/lib/prior.test.ts`                         | Tests for buildPriorFromInputs and Student-t scale correctness   | VERIFIED   | 7-test describe block; asserts `0.04079`, `0.03492`, null fallback parity    |
| `src/lib/formatting.ts`                         | `formatThreshold()` helper                                       | VERIFIED   | Exported at line 190; strict union type; Intl.NumberFormat for dollars       |
| `src/lib/formatting.test.ts`                    | Tests for formatThreshold                                        | VERIFIED   | 7 tests: `'+$10,000'`, `'+2.5% lift'`, `'Any positive impact'`, zero/negative cases |
| `src/lib/workers/evsi.worker.ts`                | Worker with truncation-aware routing for Normal priors           | VERIFIED   | Imports `computeInfeasibleTailMass`, `TRUNCATION_THRESHOLD`; uses `<=` operator |
| `src/lib/calculations/evsi.ts`                  | `computePosteriorMean` with truncated-Normal posterior           | VERIFIED   | Lines 375-388: infeasible mass check, then `truncatedNormalMeanTwoSided` call |
| `src/lib/calculations/evsi.test.ts`             | Worker routing tests + posterior truncation tests                | VERIFIED   | `'Worker truncation routing for Normal priors'` and `'Truncated-Normal posterior mean (audit P3)'` describe blocks |
| `src/hooks/useEVSICalculations.ts`              | Hook without legacy CoD; with merged warnings                    | VERIFIED   | No `CoDResults`/`calculateCostOfDelay`; `warnings: CalculationWarning[]` in results type |
| `src/components/forms/ExperimentDesignForm.tsx` | Corrected dailyTraffic label and tooltip                         | VERIFIED   | Line 190: "Total daily visitors"; line 192: "before experiment eligibility filtering" |
| `src/components/results/AdvancedResultsSection.tsx` | formatThreshold + buildPriorFromInputs + results.warnings    | VERIFIED   | All three wired at lines 28, 31, 96, 136                                     |
| `src/components/export/ExportCard.tsx`          | formatThreshold wired; cod prop removed                          | VERIFIED   | `formatThreshold` imported and called at line 141; no `cod?:` in props       |
| `src/components/export/ExportButton.tsx`        | No cod prop passed; thresholdUnit included                       | VERIFIED   | No `cod=\{` assignment; `thresholdUnit` in thresholdDisplay memo             |

**Deleted artifacts (confirmed absent):**

| Artifact                                               | Expected | Status  |
|--------------------------------------------------------|----------|---------|
| `src/lib/calculations/truncated-normal.ts`             | DELETED  | DELETED |
| `src/lib/calculations/truncated-normal.test.ts`        | DELETED  | DELETED |

---

### Key Link Verification

| From                                           | To                                          | Via                              | Status   | Details                                               |
|------------------------------------------------|---------------------------------------------|----------------------------------|----------|-------------------------------------------------------|
| `useEVSICalculations.ts`                       | `src/lib/prior.ts`                          | `import buildPriorFromInputs`    | WIRED    | Line 30: `import { buildPriorFromInputs } from '@/lib/prior'`; used at line 138 |
| `UncertaintyPriorForm.tsx`                     | `src/lib/prior.ts`                          | `import buildPriorFromInputs`    | WIRED    | Line 39: imported; used at line 615                   |
| `AdvancedResultsSection.tsx`                   | `src/lib/prior.ts`                          | `import buildPriorFromInputs`    | WIRED    | Line 31: imported; used at line 45                    |
| `AdvancedResultsSection.tsx`                   | `src/lib/formatting.ts`                     | `import formatThreshold`         | WIRED    | Line 28: imported; used at line 136                   |
| `ExportCard.tsx`                               | `src/lib/formatting.ts`                     | `import formatThreshold`         | WIRED    | Line 27: imported; used at line 141                   |
| `evsi.worker.ts`                               | `src/lib/calculations/feasibility.ts`       | `import computeInfeasibleTailMass, TRUNCATION_THRESHOLD` | WIRED | Line 24: imported; used at line 54-58 |
| `evsi.ts` (computePosteriorMean)               | `truncatedNormalMeanTwoSided` (same file)   | direct call when truncation active | WIRED | Line 383: called when `infeasibleMass > TRUNCATION_THRESHOLD` |
| `useEVSICalculations.ts` (finalResults)        | `netValueResults.warnings`                  | merge loop                        | WIRED    | Line 364: `const netValueWarnings = netValueResults.warnings ?? []` |
| `useEVSICalculations.ts`                       | `AdvancedResultsSection.tsx`                | `results.warnings` (not `results.evsi.warnings`) | WIRED | Line 96-101: `results?.warnings && results.warnings.length > 0` |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies calculation library functions, formatting helpers, and wiring. No new data-fetching components were introduced. The key data flows (prior -> EVSI -> results, warnings -> UI) are verified via wiring checks above.

---

### Behavioral Spot-Checks

| Behavior                                              | Command                                                                 | Result          | Status |
|-------------------------------------------------------|-------------------------------------------------------------------------|-----------------|--------|
| All tests pass (no regressions)                       | `npx vitest run`                                                        | 463 pass, 0 fail | PASS  |
| TypeScript compiles cleanly                           | `npx tsc --noEmit`                                                      | 0 errors        | PASS   |
| Student-t sigma_L test asserts ~0.04079               | `npx vitest run src/lib/prior.test.ts`                                  | Passes          | PASS   |
| Worker truncation routing tests pass                  | `npx vitest run src/lib/calculations/evsi.test.ts`                      | Passes          | PASS   |
| formatThreshold '+$10,000' test passes                | `npx vitest run src/lib/formatting.test.ts`                             | Passes          | PASS   |

---

### Requirements Coverage

The AUDIT-P requirement IDs are defined and tracked solely in ROADMAP.md (not in a separate REQUIREMENTS.md block — REQUIREMENTS.md uses a different ID scheme). All 9 AUDIT-P IDs are listed in ROADMAP.md under phase 22.1 and covered across the 5 plans:

| Requirement | Source Plan | Description                                              | Status     | Evidence                                                             |
|-------------|-------------|----------------------------------------------------------|------------|----------------------------------------------------------------------|
| AUDIT-P1    | 22.1-01     | Student-t double-division bug                            | SATISFIED  | `buildPriorFromInputs` uses `computeStudentTPriorScale` without pre-dividing |
| AUDIT-P2    | 22.1-04     | Worker truncation routing (Normal fast path bypass)      | SATISFIED  | Worker has `computeInfeasibleTailMass <= TRUNCATION_THRESHOLD` gate  |
| AUDIT-P3    | 22.1-05     | Truncated-Normal posterior mean for Normal under truncation | SATISFIED | `computePosteriorMean` calls `truncatedNormalMeanTwoSided` when active |
| AUDIT-P4    | 22.1-01     | Centralized prior construction                           | SATISFIED  | All 3 call sites use `buildPriorFromInputs`                          |
| AUDIT-P5    | 22.1-02     | Unit-aware threshold display (dollars vs percentage)     | SATISFIED  | `formatThreshold()` with Intl.NumberFormat, wired to 3 display sites |
| AUDIT-P6    | 22.1-02     | dailyTraffic semantics (total before eligibility)        | SATISFIED  | Label "Total daily visitors", tooltip clarifies pre-eligibility      |
| AUDIT-P7    | 22.1-03     | Legacy CoD removal                                       | SATISFIED  | No `calculateCostOfDelay`, `CoDResults`, or `cod=` in live code      |
| AUDIT-P8a   | 22.1-04     | Dead `truncated-normal.ts` module deleted                | SATISFIED  | File does not exist on disk                                           |
| AUDIT-P8b   | 22.1-05     | Net-value warnings plumbed to UI                         | SATISFIED  | `EVSICalculationResults.warnings` merged and rendered                |

**Orphaned requirements:** None. All 9 AUDIT-P IDs claimed by a plan and verified in codebase.

---

### Anti-Patterns Found

| File | Pattern | Severity | Verdict |
|------|---------|----------|---------|
| `useEVSICalculations.ts` line 35 | Comment references `CoDResults` and `calculateCostOfDelay` | Info | Not a stub — it is a removal notice comment explaining what was deleted. No code impact. |
| `useEVSICalculations.test.ts` line 226 | Comment references `codDollars` | Info | Explanatory comment in a test, no live code path. |

No blocker or warning-severity anti-patterns found.

---

### Human Verification Required

**1. Dollar threshold renders as currency in actual UI**
- **Test:** Set up an A/B test scenario with a dollar-value threshold (e.g., $10,000), run to Results section, inspect the threshold display card and exported PDF/PNG.
- **Expected:** Value shown as "+$10,000" not "+10000%" in both results and export.
- **Why human:** Intl.NumberFormat output is locale-sensitive; visual rendering in PDF export requires manual inspection.

**2. Warning banner appears for edge-case inputs**
- **Test:** Enter a high baseline conversion rate (e.g., 90%) with a Normal prior. Navigate to Results.
- **Expected:** A warning banner appears indicating truncation or low-acceptance conditions.
- **Why human:** Warning rendering depends on runtime Monte Carlo simulation results which cannot be invoked without a running browser.

**3. dailyTraffic tooltip copy is correct in rendered UI**
- **Test:** In ExperimentDesignForm, hover over the "Total daily visitors" field tooltip.
- **Expected:** Tooltip text says "Total daily visitors to the site or app, before experiment eligibility filtering. Use Eligible traffic below to specify the share that qualifies."
- **Why human:** Tooltip rendering requires browser interaction.

---

### Gaps Summary

No gaps. All 8 success criteria verified programmatically. All 9 AUDIT-P requirement IDs satisfied. Full test suite passes (463/463). TypeScript clean. All commits present and verified in git log.

**Commits verified:**
- `8243d58` — buildPriorFromInputs + Student-t tests (Plan 01, Task 1)
- `e6de2a6` — 3 call sites migrated to buildPriorFromInputs (Plan 01, Task 2)
- `6820376` — formatThreshold() helper + 7 tests (Plan 02, Task 1)
- `28cd2cd` — threshold display sites + dailyTraffic label fix (Plan 02, Task 2)
- `dd8860f` — worker truncation gate + routing tests (Plan 04, Task 1)
- `b4223ea` — truncated-normal.ts deleted (Plan 04, Task 2)
- `a729ad1` — truncated-Normal posterior mean + 4 tests (Plan 05, Task 1)
- `fb006fd` — net-value warnings plumbed to UI (Plan 05, Task 2)
- `e3f9b4c` — legacy CoD removed from hook (Plan 03, Task 1)
- `c1b095e` — cod prop removed from export pipeline (Plan 03, Task 2)

---

_Verified: 2026-03-25_
_Verifier: Claude (gsd-verifier)_
