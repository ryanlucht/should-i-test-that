---
phase: 22.1-stats-engine-correctness-fixes
plan: 02
subsystem: ui, formatting
tags: [intl, formatting, threshold, audit-p5, audit-p6]

# Dependency graph
requires:
  - phase: 22.1-01
    provides: buildPriorFromInputs centralized prior construction
provides:
  - formatThreshold() helper for unit-aware threshold display
  - Corrected dailyTraffic label and tooltip semantics
affects: [export, results, forms]

# Tech tracking
tech-stack:
  added: []
  patterns: [Intl.NumberFormat for currency formatting in formatThreshold]

key-files:
  created: []
  modified:
    - src/lib/formatting.ts
    - src/lib/formatting.test.ts
    - src/components/results/AdvancedResultsSection.tsx
    - src/components/export/ExportButton.tsx
    - src/components/export/ExportCard.tsx
    - src/components/forms/ExperimentDesignForm.tsx

key-decisions:
  - "formatThreshold uses strict union type for scenario (no | string) with safe runtime fallback"
  - "dailyTraffic tooltip references 'Eligible traffic' field name to guide users"

patterns-established:
  - "formatThreshold: central helper for all threshold display across results and export"

requirements-completed: [AUDIT-P5, AUDIT-P6]

# Metrics
duration: 4min
completed: 2026-03-25
---

# Phase 22.1 Plan 02: Threshold Display & Traffic Semantics Summary

**Unit-aware formatThreshold() helper (dollars as currency, lift as percentage) with 3 display sites migrated and dailyTraffic label/tooltip corrected to clarify total-before-eligibility semantics**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-25T20:20:43Z
- **Completed:** 2026-03-25T20:25:02Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Created `formatThreshold()` helper with strict union type, Intl.NumberFormat currency formatting, and safe runtime fallback for unknown scenarios
- Replaced 3 threshold display sites (AdvancedResultsSection, ExportCard, ExportButton) with `formatThreshold()` calls
- Updated dailyTraffic label to "Total daily visitors" and tooltip to clarify total traffic before eligibility filtering
- All 7 test cases pass: dollar, lift, any-positive, negative, and zero edge cases

## Task Commits

Each task was committed atomically:

1. **Task 1: Create formatThreshold() helper with tests** - `6820376` (feat) - TDD RED->GREEN
2. **Task 2: Replace threshold display sites and fix dailyTraffic labels** - `28cd2cd` (fix)

## Files Created/Modified
- `src/lib/formatting.ts` - Added formatThreshold() helper function
- `src/lib/formatting.test.ts` - Added 7 test cases for formatThreshold
- `src/components/results/AdvancedResultsSection.tsx` - Replaced inline threshold formatting with formatThreshold(), removed thresholdLift variable
- `src/components/export/ExportButton.tsx` - Added thresholdUnit to thresholdDisplay memo
- `src/components/export/ExportCard.tsx` - Updated threshold prop interface, replaced inline formatting with formatThreshold()
- `src/components/forms/ExperimentDesignForm.tsx` - Updated dailyTraffic label and tooltip, updated header comment

## Decisions Made
- `formatThreshold` uses strict union type (`'any-positive' | 'minimum-lift' | 'accept-loss'`) with NO trailing `| string`, plus a safe runtime fallback for unexpected values
- dailyTraffic tooltip says "Use Eligible traffic below to specify the share that qualifies" to direct users to the eligibility fraction field

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- formatThreshold() is available for any future threshold display sites
- Prior construction (plan 01) and threshold formatting (plan 02) foundations complete
- Ready for plan 03 (legacy CoD removal) and remaining engine fixes

## Self-Check: PASSED

All 6 files verified present. Both commit hashes (6820376, 28cd2cd) found in git log.

---
*Phase: 22.1-stats-engine-correctness-fixes*
*Completed: 2026-03-25*
