---
phase: 16-druids-foundation-copy-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
autonomous: true

must_haves:
  truths:
    - "Cards render with shadow-card styling (0 1px 3px 0 rgba(0, 0, 0, 0.1))"
    - "Cards have border-dd-border (#E5E7EB) border"
    - "Inputs have focus states with border-dd-grape and ring-dd-grape"
  artifacts:
    - path: "src/index.css"
      provides: "shadow-card and shadow-floating utility definitions"
      contains: "shadow-card"
    - path: "src/components/ui/card.tsx"
      provides: "Card component with DRUIDS shadow"
      contains: "shadow-card"
    - path: "src/components/ui/input.tsx"
      provides: "Input component with DRUIDS focus states"
      contains: "focus-visible:border-primary"
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "shadow-card class"
      via: "className"
      pattern: "shadow-card"
---

<objective>
Apply DRUIDS card shadows and input styling patterns.

Purpose: Complete the visual foundation by adding the specific shadow and focus state patterns from the DRUIDS mockup.

Output: Updated Card and Input components with DRUIDS-specific styling.
</objective>

<execution_context>
@/Users/ryan.lucht/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryan.lucht/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/mockups/code.html

Source files:
@src/index.css
@src/components/ui/card.tsx
@src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DRUIDS shadow utilities to CSS</name>
  <files>src/index.css</files>
  <action>
Add DRUIDS-specific shadow utilities after the existing custom classes in index.css.

Find the "Design System Custom Classes" section and add these shadow classes:

```css
/* DRUIDS Shadow System */
.shadow-card {
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px -1px rgba(0, 0, 0, 0.06);
}

.shadow-floating {
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.05);
}
```

These match exactly the shadow values from .planning/mockups/code.html tailwind config.
  </action>
  <verify>
Run `npm run build` to confirm CSS compiles.
Run `npm run lint` to check for issues.
  </verify>
  <done>
shadow-card and shadow-floating utility classes added to index.css.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Card component with DRUIDS styling</name>
  <files>src/components/ui/card.tsx</files>
  <action>
Update the Card component to use shadow-card instead of shadow-sm.

Current Card className:
```tsx
"bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm"
```

Change to:
```tsx
"bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-card"
```

This provides the slightly more pronounced shadow from DRUIDS spec while maintaining existing styling.
  </action>
  <verify>
Run `npm run build` to ensure no TypeScript errors.
Run `npm run test` to ensure no regressions.
Start dev server and visually inspect cards - should have subtle shadow.
  </verify>
  <done>
Card component uses shadow-card class from DRUIDS shadow system.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Input component focus states</name>
  <files>src/components/ui/input.tsx</files>
  <action>
Review Input component focus states and ensure they align with DRUIDS patterns.

Current Input focus classes:
```tsx
"focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]"
```

Since --ring is set to #7C3AED (dd-grape) in index.css, this already matches DRUIDS focus pattern:
- border-dd-grape on focus (via border-ring)
- ring-dd-grape with opacity (via ring-ring/50)

**Verify this is working correctly by checking that --ring = #7C3AED in index.css.**

If needed, add a comment to Input component documenting the DRUIDS alignment:
```tsx
// DRUIDS focus states: border-dd-grape, ring-dd-grape via --ring token
```

Add the comment before the className cn() call.
  </action>
  <verify>
Run `npm run build` to confirm no errors.
Run `npm run test` to ensure no regressions.
Manually test input focus in browser - should show purple border and ring.
  </verify>
  <done>
Input component focus states verified to use DRUIDS dd-grape color via --ring token.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - should complete without errors
2. Run `npm run lint` - should pass
3. Run `npm run test` - all tests should pass
4. Visual check in dev server:
   - Cards should have visible but subtle shadow (shadow-card)
   - Input fields on focus should have purple border and ring
   - VerdictCard, ResultsSection, SupportingCard should all render with shadow-card
</verification>

<success_criteria>
- src/index.css contains shadow-card and shadow-floating utility classes
- Card component uses shadow-card class
- Input focus states use dd-grape (#7C3AED) color via --ring token
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/16-druids-foundation-copy-audit/16-02-SUMMARY.md`
</output>
