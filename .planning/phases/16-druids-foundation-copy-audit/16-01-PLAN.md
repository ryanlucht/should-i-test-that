---
phase: 16-druids-foundation-copy-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - index.html
autonomous: true

must_haves:
  truths:
    - "Primary accent color renders as #7C3AED on all interactive elements"
    - "Text colors follow DRUIDS hierarchy (dd-text #111827, dd-textSecondary #4B5563, dd-muted #6B7280)"
    - "Inter font loads with correct weights (400, 500, 600, 700) from Google Fonts"
  artifacts:
    - path: "src/index.css"
      provides: "DRUIDS color tokens and font declaration"
      contains: "--foreground: #111827"
    - path: "index.html"
      provides: "Google Fonts preconnect and stylesheet link for Inter"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "index.html"
      to: "fonts.googleapis.com"
      via: "link rel=stylesheet"
      pattern: "Inter.*wght@400;500;600;700"
---

<objective>
Apply DRUIDS color tokens and Inter font to the design system foundation.

Purpose: Establish the visual foundation for the DRUIDS design refresh by aligning all color tokens with the mockup specifications and properly loading the Inter font family.

Output: Updated index.css with DRUIDS-aligned color tokens and index.html with Google Fonts import.
</objective>

<execution_context>
@/Users/ryan.lucht/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryan.lucht/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/mockups/code.html

Source files:
@src/index.css
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Google Fonts import for Inter</name>
  <files>index.html</files>
  <action>
Add Google Fonts link for Inter font in index.html head section:

1. Add preconnect hints for performance (before other head content):
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
```

2. Add stylesheet link for Inter with weights 400, 500, 600, 700:
```html
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```

Place these before the existing meta viewport tag for proper loading order.
  </action>
  <verify>
Open index.html and confirm the three link tags are present in the head section.
Run `npm run build` to ensure no HTML parsing errors.
  </verify>
  <done>
index.html contains Google Fonts preconnect and Inter stylesheet link with weights 400, 500, 600, 700.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update color tokens to DRUIDS specification</name>
  <files>src/index.css</files>
  <action>
Update the :root CSS custom properties to use exact DRUIDS hex values instead of oklch approximations.

**Light theme (:root) updates:**

Current values to replace:
```css
--background: oklch(0.99 0.002 250);
--foreground: oklch(0.15 0.02 250);
--card: oklch(1 0 0);
--card-foreground: oklch(0.15 0.02 250);
--popover: oklch(1 0 0);
--popover-foreground: oklch(0.15 0.02 250);
--secondary: oklch(0.95 0.01 250);
--secondary-foreground: oklch(0.2 0.02 250);
--muted: oklch(0.95 0.01 250);
--muted-foreground: oklch(0.5 0.02 250);
--accent: oklch(0.95 0.01 250);
--accent-foreground: oklch(0.2 0.02 250);
--border: oklch(0.9 0.01 250);
--input: oklch(0.9 0.01 250);
```

New values (DRUIDS tokens from mockup):
```css
/* DRUIDS tokens - exactly from .planning/mockups/code.html */
--background: #F9FAFB;         /* dd-bg - Main Background */
--foreground: #111827;         /* dd-text - Main Text */
--card: #FFFFFF;               /* dd-card - Card Background */
--card-foreground: #111827;    /* dd-text */
--popover: #FFFFFF;            /* dd-card */
--popover-foreground: #111827; /* dd-text */
--secondary: #F3F4F6;          /* dd-cardHover - Light Gray */
--secondary-foreground: #111827;
--muted: #F3F4F6;              /* dd-cardHover */
--muted-foreground: #6B7280;   /* dd-muted - Muted Text */
--accent: #F3E8FF;             /* dd-grapeLight - Purple tint */
--accent-foreground: #111827;
--border: #E5E7EB;             /* dd-border - Subtle Border */
--input: #E5E7EB;              /* dd-border */
```

Also update the success/destructive colors:
```css
--destructive: #EF4444;        /* Standard red */
```

Add new DRUIDS-specific tokens after --ring:
```css
/* DRUIDS additional tokens */
--dd-text-secondary: #4B5563;  /* dd-textSecondary */
--dd-grape-dark: #6D28D9;      /* dd-grapeDark */
--dd-success-bg: #ECFDF5;      /* dd-successBg */
--dd-success-border: #A7F3D0;  /* dd-successBorder */
--dd-info: #3B82F6;            /* dd-info */
--dd-info-bg: #EFF6FF;         /* dd-infoBg */
```

**Update the design system header comment** to reflect DRUIDS tokens:
Replace the existing color documentation comment block with:
```css
/*
 * DRUIDS Design System - Datadog-inspired
 *
 * Design spec: .planning/mockups/code.html
 *
 * Color tokens:
 * - Primary: #7C3AED (dd-grape) - CTAs, selection states
 * - Primary dark: #6D28D9 (dd-grapeDark) - hover states
 * - Primary light: #F3E8FF (dd-grapeLight) - backgrounds
 * - Background: #F9FAFB (dd-bg) - page background
 * - Card: #FFFFFF (dd-card) - card surfaces
 * - Border: #E5E7EB (dd-border) - subtle borders
 * - Text: #111827 (dd-text) - primary text
 * - Text secondary: #4B5563 (dd-textSecondary) - secondary text
 * - Muted: #6B7280 (dd-muted) - muted/placeholder text
 * - Success: #10B981 (dd-success) - success states
 * - Info: #3B82F6 (dd-info) - info callouts
 *
 * Font: Inter (400, 500, 600, 700)
 */
```

**Keep dark mode colors as-is** for now (they're reasonable approximations).
  </action>
  <verify>
Run `npm run build` to ensure CSS parses correctly.
Run `npm run lint` to check for any issues.
Check that the build output contains the new hex color values.
  </verify>
  <done>
src/index.css contains DRUIDS color tokens with exact hex values: #111827 for foreground, #6B7280 for muted-foreground, #E5E7EB for border, #F9FAFB for background.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add text-secondary utility class</name>
  <files>src/index.css</files>
  <action>
Add utility class for dd-textSecondary (#4B5563) in the custom classes section after the existing semantic classes.

Find the "Design System Custom Classes" section and add:
```css
/* Text secondary color - #4B5563 */
.text-secondary {
  color: #4B5563;
}
```

This allows components to use `text-secondary` for the DRUIDS secondary text color, which is distinct from `text-muted-foreground` (#6B7280).

NOTE: Do not modify the @theme inline block - the new --dd-text-secondary variable is for reference, components should use the utility class directly or the muted-foreground which is close enough for most uses.
  </action>
  <verify>
Run `npm run build` to confirm CSS compiles.
Run `npm run test` to ensure no regressions.
  </verify>
  <done>
text-secondary utility class added to index.css providing #4B5563 color.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - should complete without errors
2. Run `npm run lint` - should pass
3. Run `npm run test` - all tests should pass
4. Start dev server (`npm run dev`) and visually inspect:
   - Text should render in dark gray (#111827)
   - Muted text should render in medium gray (#6B7280)
   - Primary purple elements should be #7C3AED
   - Inter font should load (check Network tab in DevTools)
</verification>

<success_criteria>
- index.html contains Google Fonts import for Inter with weights 400, 500, 600, 700
- src/index.css :root contains hex values: --foreground: #111827, --muted-foreground: #6B7280, --border: #E5E7EB
- Build completes without errors
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-druids-foundation-copy-audit/16-01-SUMMARY.md`
</output>
