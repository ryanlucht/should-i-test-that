---
phase: 19-basic-mode-deprecation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/calculations/evpi.ts
  - src/lib/calculations/evpi.test.ts
  - src/lib/calculations/index.ts
  - src/lib/calculations/types.ts
  - src/hooks/useEVPICalculations.ts
  - src/hooks/useEVPICalculations.test.ts
  - src/components/results/ResultsSection.tsx
  - src/components/results/ResultsSection.test.tsx
  - src/components/results/VerdictCard.tsx
  - src/components/results/CostOfDelayCard.tsx
  - src/components/results/index.ts
  - src/lib/calculations/cost-of-delay.ts
  - src/lib/calculations/cost-of-delay.test.ts
autonomous: true
requirements:
  - DEPR-02
must_haves:
  truths:
    - "No EVPI calculation code remains in the codebase"
    - "No CostOfDelayCard component exists"
    - "No VerdictCard (EVPI verdict) component exists"
    - "No Basic mode ResultsSection component exists"
    - "No useEVPICalculations hook exists"
    - "Existing Advanced mode calculations and components still work"
  artifacts:
    - path: "src/lib/calculations/index.ts"
      provides: "Barrel export without evpi or cost-of-delay re-exports"
      contains: "export * from './evsi'"
    - path: "src/components/results/index.ts"
      provides: "Barrel export without VerdictCard, CostOfDelayCard, or ResultsSection"
      contains: "export { AdvancedResultsSection }"
  key_links:
    - from: "src/components/results/index.ts"
      to: "AdvancedResultsSection"
      via: "barrel export"
      pattern: "export.*AdvancedResultsSection"
---

<objective>
Remove all EVPI calculation code, Basic mode results components, and their tests from the codebase.

Purpose: DEPR-02 requires removing EVPI calculation code, components (CostOfDelayCard, VerdictCard), and related tests. This is the foundational cleanup that later mode-infrastructure removal depends on.

Output: EVPI engine files deleted, Basic results components deleted, barrel exports updated, all remaining tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete EVPI calculation files, hook, and their tests</name>
  <files>
    src/lib/calculations/evpi.ts
    src/lib/calculations/evpi.test.ts
    src/lib/calculations/cost-of-delay.ts
    src/lib/calculations/cost-of-delay.test.ts
    src/lib/calculations/index.ts
    src/lib/calculations/types.ts
    src/hooks/useEVPICalculations.ts
    src/hooks/useEVPICalculations.test.ts
  </files>
  <read_first>
    src/lib/calculations/evpi.ts
    src/lib/calculations/evpi.test.ts
    src/lib/calculations/cost-of-delay.ts
    src/lib/calculations/cost-of-delay.test.ts
    src/lib/calculations/index.ts
    src/lib/calculations/types.ts
    src/hooks/useEVPICalculations.ts
    src/hooks/useEVPICalculations.test.ts
    src/hooks/useEVSICalculations.ts
    src/components/results/AdvancedResultsSection.tsx
  </read_first>
  <action>
    1. DELETE the following files entirely:
       - `src/lib/calculations/evpi.ts` (EVPI calculation engine)
       - `src/lib/calculations/evpi.test.ts` (EVPI tests)
       - `src/hooks/useEVPICalculations.ts` (EVPI React hook)
       - `src/hooks/useEVPICalculations.test.ts` (EVPI hook tests)

    2. DELETE standalone Cost of Delay files (per DEPR-02, standalone CoD exports are EVPI-mode artifacts):
       - `src/lib/calculations/cost-of-delay.ts`
       - `src/lib/calculations/cost-of-delay.test.ts`
       NOTE: The CostOfDelayCard component imports `CoDResults` from cost-of-delay.ts, but CostOfDelayCard is being deleted in Task 2. The integrated CoD calculation in `net-value.ts` (used by Advanced/EVSI mode) does NOT import from cost-of-delay.ts -- it has its own inline implementation. Verify this before deleting.

    3. UPDATE `src/lib/calculations/index.ts` -- remove these two export lines:
       - Remove: `export * from './evpi';`
       - Remove: `export * from './cost-of-delay';`
       Keep all other exports (statistics, derived, chart-data, distributions, sample-size, evsi, net-value, abtest-math).

    4. UPDATE `src/lib/calculations/types.ts` -- remove the `EVPIInputs` and `EVPIResults` interfaces and related types:
       - Remove: `interface EVPIInputs` (lines 16-35)
       - Remove: `interface EVPIResults` (lines 43-95)
       - Remove: `interface TruncatedDiagnostics` (lines 104-116)
       - Remove: `interface EdgeCaseFlags` (lines 123-141)
       - Keep: `PriorDistribution` import/re-export, `EVSIInputs`, `EVSIResults`, `CalculationWarning`, `NetValueInputs`, `NetValueResults`
       - Update the file header comment to remove "EVPI" references

    5. Run `npx vitest --run` to confirm no test failures from EVPI/CoD-related imports. If any remaining files import from deleted modules, fix those imports. The key files that might still reference EVPIResults are:
       - `src/components/export/ExportButton.tsx` (imports `EVPIResults` -- will be handled in Plan 02)
       - `src/components/results/ResultsSection.tsx` (imports `useEVPICalculations` -- being deleted in Task 2)
       For now, if TypeScript compilation fails due to these downstream consumers, that is expected -- they will be cleaned up in Task 2 of this plan. Run vitest with `--reporter=verbose` to isolate failures to only the expected files.
  </action>
  <verify>
    <automated>ls src/lib/calculations/evpi.ts src/lib/calculations/evpi.test.ts src/lib/calculations/cost-of-delay.ts src/lib/calculations/cost-of-delay.test.ts src/hooks/useEVPICalculations.ts src/hooks/useEVPICalculations.test.ts 2>&1 | grep -c "No such file" | grep -q "6" && echo "All 6 EVPI/CoD files deleted" || echo "FAIL: Not all files deleted"</automated>
  </verify>
  <acceptance_criteria>
    - Files `src/lib/calculations/evpi.ts`, `evpi.test.ts`, `cost-of-delay.ts`, `cost-of-delay.test.ts` do not exist
    - Files `src/hooks/useEVPICalculations.ts`, `useEVPICalculations.test.ts` do not exist
    - `src/lib/calculations/index.ts` does NOT contain `export * from './evpi'`
    - `src/lib/calculations/index.ts` does NOT contain `export * from './cost-of-delay'`
    - `src/lib/calculations/index.ts` DOES contain `export * from './evsi'`
    - `src/lib/calculations/types.ts` does NOT contain `interface EVPIInputs`
    - `src/lib/calculations/types.ts` does NOT contain `interface EVPIResults`
    - `src/lib/calculations/types.ts` DOES contain `interface EVSIInputs`
    - `src/lib/calculations/types.ts` DOES contain `interface EVSIResults`
  </acceptance_criteria>
  <done>All EVPI calculation code, standalone Cost of Delay code, EVPI hook, and their tests are deleted. Barrel exports updated. Types file contains only EVSI-related types.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Basic mode results components and update results barrel</name>
  <files>
    src/components/results/ResultsSection.tsx
    src/components/results/ResultsSection.test.tsx
    src/components/results/VerdictCard.tsx
    src/components/results/CostOfDelayCard.tsx
    src/components/results/index.ts
  </files>
  <read_first>
    src/components/results/ResultsSection.tsx
    src/components/results/ResultsSection.test.tsx
    src/components/results/VerdictCard.tsx
    src/components/results/CostOfDelayCard.tsx
    src/components/results/index.ts
    src/components/results/AdvancedResultsSection.tsx
    src/components/results/EVSIVerdictCard.tsx
    src/components/results/ValueBreakdownCard.tsx
    src/components/results/SupportingCard.tsx
  </read_first>
  <action>
    1. DELETE the following files entirely:
       - `src/components/results/ResultsSection.tsx` (Basic mode EVPI results display)
       - `src/components/results/ResultsSection.test.tsx` (its accessibility tests)
       - `src/components/results/VerdictCard.tsx` (EVPI verdict card with "Advanced mode" CTA)
       - `src/components/results/CostOfDelayCard.tsx` (standalone CoD expandable card)

    2. UPDATE `src/components/results/index.ts` to remove deleted exports. The file should become:
       ```typescript
       export { SupportingCard } from './SupportingCard';
       export { EVSIVerdictCard } from './EVSIVerdictCard';
       export { ValueBreakdownCard } from './ValueBreakdownCard';
       export { AdvancedResultsSection } from './AdvancedResultsSection';
       ```
       Remove these lines:
       - `export { VerdictCard } from './VerdictCard';`
       - `export { ResultsSection } from './ResultsSection';`
       - `export { CostOfDelayCard } from './CostOfDelayCard';`

    3. Run `npx vitest --run --reporter=verbose` -- expect possible failures in:
       - `src/App.test.tsx` (may reference mode selection or Basic mode-specific behavior)
       - `src/pages/CalculatorPage.tsx` (imports ResultsSection -- cleaned in Plan 02)
       - `src/components/export/ExportButton.tsx` (references BasicModeProps with EVPIResults -- cleaned in Plan 02)
       These downstream consumer failures are expected and will be resolved in Plan 02. Verify no OTHER unexpected failures.
  </action>
  <verify>
    <automated>ls src/components/results/ResultsSection.tsx src/components/results/VerdictCard.tsx src/components/results/CostOfDelayCard.tsx 2>&1 | grep -c "No such file" | grep -q "3" && echo "All 3 Basic results components deleted" || echo "FAIL: Not all files deleted"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/results/ResultsSection.tsx` does not exist
    - File `src/components/results/ResultsSection.test.tsx` does not exist
    - File `src/components/results/VerdictCard.tsx` does not exist
    - File `src/components/results/CostOfDelayCard.tsx` does not exist
    - `src/components/results/index.ts` does NOT contain `VerdictCard`
    - `src/components/results/index.ts` does NOT contain `ResultsSection`
    - `src/components/results/index.ts` does NOT contain `CostOfDelayCard`
    - `src/components/results/index.ts` DOES contain `AdvancedResultsSection`
    - `src/components/results/index.ts` DOES contain `EVSIVerdictCard`
    - `src/components/results/index.ts` DOES contain `SupportingCard`
  </acceptance_criteria>
  <done>All Basic mode results components (ResultsSection, VerdictCard, CostOfDelayCard) and their tests are deleted. Results barrel export updated to only include Advanced/EVSI components.</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -r "calculateEVPI\|useEVPICalculations\|EVPIInputs\|VerdictCard\|CostOfDelayCard\b\|ResultsSection\b" src/ --include="*.ts" --include="*.tsx" -l` should return only files that will be updated in Plan 02 (CalculatorPage.tsx, ExportButton.tsx, ExportCard.tsx, and possibly AdvancedResultsSection.test.tsx)
- No EVPI calculation files or Basic results component files exist
- Barrel exports are updated
</verification>

<success_criteria>
- All EVPI calculation code (evpi.ts, cost-of-delay.ts) deleted
- All EVPI-specific components (VerdictCard, CostOfDelayCard, ResultsSection) deleted
- All corresponding test files deleted
- Barrel exports updated to exclude deleted modules
- Types file retains only EVSI-related types
- Advanced mode calculations (evsi.ts, net-value.ts) are unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/19-basic-mode-deprecation/19-01-SUMMARY.md`
</output>
