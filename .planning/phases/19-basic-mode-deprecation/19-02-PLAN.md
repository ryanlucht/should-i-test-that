---
phase: 19-basic-mode-deprecation
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/types/wizard.ts
  - src/stores/wizardStore.ts
  - src/stores/wizardStore.test.ts
  - src/pages/WelcomePage.tsx
  - src/pages/CalculatorPage.tsx
  - src/components/welcome/ModeCard.tsx
  - src/components/wizard/ModeToggle.tsx
  - src/components/forms/UncertaintyPriorForm.tsx
  - src/components/export/ExportButton.tsx
  - src/components/export/ExportCard.tsx
  - src/hooks/useExportPng.ts
  - src/hooks/useEVSICalculations.ts
  - src/hooks/useEVSICalculations.test.ts
  - src/lib/analytics.ts
  - src/App.tsx
  - src/App.test.tsx
  - src/components/results/AdvancedResultsSection.tsx
  - src/components/results/AdvancedResultsSection.test.tsx
autonomous: true
requirements:
  - DEPR-01
  - DEPR-03
must_haves:
  truths:
    - "Welcome screen has no Basic/Advanced mode selector; clicking Get Started goes directly to EVSI calculator"
    - "No mode toggle exists in the calculator header"
    - "No dual-mode conditional rendering exists anywhere"
    - "No sessionStorage backups for mode switches exist"
    - "All existing EVSI/Advanced inputs and calculations still work correctly"
    - "All tests pass (vitest exits 0)"
  artifacts:
    - path: "src/types/wizard.ts"
      provides: "Wizard types without Mode type or dual-mode infrastructure"
    - path: "src/stores/wizardStore.ts"
      provides: "Store without setMode, mode field, or sessionStorage mode backups"
    - path: "src/pages/CalculatorPage.tsx"
      provides: "Calculator with single section list (5 sections: Baseline, Uncertainty, Threshold, Test Design, Results) and only AdvancedResultsSection"
    - path: "src/pages/WelcomePage.tsx"
      provides: "Welcome page without ModeSelection component"
  key_links:
    - from: "src/pages/CalculatorPage.tsx"
      to: "AdvancedResultsSection"
      via: "direct import and render"
      pattern: "import.*AdvancedResultsSection"
    - from: "src/stores/wizardStore.ts"
      to: "sessionStorage"
      via: "persist middleware"
      pattern: "persist.*sessionStorage"
    - from: "src/App.tsx"
      to: "WelcomePage"
      via: "page routing"
      pattern: "WelcomePage"
---

<objective>
Remove all mode-switching infrastructure and convert the app to a single EVSI-based experience.

Purpose: DEPR-01 requires removing the mode selector from the welcome screen. DEPR-03 requires removing all mode-switching UI, localStorage/sessionStorage backups for mode switches, and dual-mode conditional rendering. After this plan, the calculator is a single experience with 5 sections (Baseline, Uncertainty, Threshold, Test Design, Results) using only EVSI calculations.

Output: Single-mode calculator with no trace of Basic/Advanced distinction. All tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-basic-mode-deprecation/19-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs from Plan 01 outcomes -->
<!-- After Plan 01, these types no longer exist: EVPIInputs, EVPIResults, EdgeCaseFlags, TruncatedDiagnostics -->
<!-- These types remain in src/lib/calculations/types.ts: -->

From src/lib/calculations/types.ts (after Plan 01):
```typescript
export interface EVSIInputs { K: number; baselineConversionRate: number; threshold_L: number; prior: PriorDistribution; n_control: number; n_variant: number; }
export interface EVSIResults { evsiDollars: number; defaultDecision: 'ship' | 'dont-ship'; probabilityClearsThreshold: number; probabilityTestChangesDecision: number; numSamples?: number; numRejected?: number; warnings?: CalculationWarning[]; }
export interface NetValueInputs { K: number; baselineConversionRate: number; threshold_L: number; prior: PriorDistribution; n_control: number; n_variant: number; testDurationDays: number; variantFraction: number; decisionLatencyDays: number; }
export interface NetValueResults { netValueDollars: number; maxTestBudgetDollars: number; defaultDecision: 'ship' | 'dont-ship'; probabilityClearsThreshold: number; probabilityTestChangesDecision: number; numSamples?: number; numRejected?: number; warnings?: CalculationWarning[]; }
```

From src/components/results/index.ts (after Plan 01):
```typescript
export { SupportingCard } from './SupportingCard';
export { EVSIVerdictCard } from './EVSIVerdictCard';
export { ValueBreakdownCard } from './ValueBreakdownCard';
export { AdvancedResultsSection } from './AdvancedResultsSection';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Mode type, mode-switching store logic, and delete ModeToggle/ModeCard components</name>
  <files>
    src/types/wizard.ts
    src/stores/wizardStore.ts
    src/stores/wizardStore.test.ts
    src/components/welcome/ModeCard.tsx
    src/components/wizard/ModeToggle.tsx
    src/lib/analytics.ts
  </files>
  <read_first>
    src/types/wizard.ts
    src/stores/wizardStore.ts
    src/stores/wizardStore.test.ts
    src/components/welcome/ModeCard.tsx
    src/components/wizard/ModeToggle.tsx
    src/lib/analytics.ts
  </read_first>
  <action>
    **A. Update `src/types/wizard.ts`:**
    1. Remove `export type Mode = 'basic' | 'advanced';` (line 14)
    2. Remove all comments referencing "basic" mode or mode switching (e.g., "basic: EVPI calculation with fewer inputs")
    3. Remove `SectionId` entries `'test-design'` and `'costs'` comments that say "Advanced only" -- they are no longer conditional
    4. Merge `SharedInputs` and `AdvancedInputs` into a single `WizardInputs` interface. The new interface should contain ALL fields from both:
       - From SharedInputs: baselineConversionRate, annualVisitors, visitorUnitLabel, valuePerConversion, priorType, priorIntervalLow, priorIntervalHigh, thresholdScenario, thresholdUnit, thresholdValue
       - From AdvancedInputs: priorShape, studentTDf, testDurationDays, dailyTraffic, trafficSplit, eligibilityFraction, conversionLatencyDays, decisionLatencyDays
       Keep all JSDoc comments on individual fields.
    5. Remove the `InputsState` interface (no longer need `shared` and `advanced` nesting)
    6. Create a single `initialInputs: WizardInputs` export combining both `initialSharedInputs` and `initialAdvancedInputs` into one flat object. The `priorShape` default should be `'normal'` (not null -- since we're always in EVSI mode now). All other defaults remain the same.
    7. Remove `initialSharedInputs` and `initialAdvancedInputs` exports
    8. Update `WizardState` to remove `mode: Mode` field. Change `inputs: InputsState` to `inputs: WizardInputs`
    9. Update `WizardActions` to:
       - Remove `setMode: (mode: Mode) => void`
       - Replace `setSharedInput` and `setAdvancedInput` with a single `setInput: <K extends keyof WizardInputs>(key: K, value: WizardInputs[K]) => void`
       - Keep `setCurrentSection`, `markSectionComplete`, `canAccessSection`, `resetWizard`
    10. Update `WizardStore = WizardState & WizardActions`

    **B. Update `src/stores/wizardStore.ts`:**
    1. Remove `import type { Mode, ... }` -- import `WizardStore, WizardInputs, initialInputs` from wizard types
    2. Remove `import { trackModeSelected } from '@/lib/analytics'`
    3. Remove the entire `setMode` function (lines 70-171) and all sessionStorage backup logic
    4. Remove `mode: 'basic'` from initial state
    5. Change `inputs: initialInputs` (using the new flat initialInputs)
    6. Replace `setSharedInput` and `setAdvancedInput` with a single `setInput` action:
       ```typescript
       setInput: <K extends keyof WizardInputs>(key: K, value: WizardInputs[K]) => {
         set((state) => ({
           inputs: { ...state.inputs, [key]: value },
         }));
       },
       ```
    7. In `resetWizard`:
       - Remove `sessionStorage.removeItem('wizard-advanced-backup')` and `sessionStorage.removeItem('wizard-basic-backup')`
       - Remove `mode: 'basic'` from the reset state
       - Set `inputs: initialInputs` (flat)
    8. In `partialize` (persist config): Remove `mode` from persisted fields. Change to just `inputs`:
       ```typescript
       partialize: (state) => ({ inputs: state.inputs }),
       ```

    **C. DELETE `src/components/welcome/ModeCard.tsx`** (entire file)

    **D. DELETE `src/components/wizard/ModeToggle.tsx`** (entire file)

    **E. Update `src/lib/analytics.ts`:**
    1. Remove the `trackModeSelected` function entirely (lines 29-37)
    2. Update `trackCalculationCompleted` to only accept `'EVSI'` type (remove `'EVPI'` from union): `calculationType: 'EVSI'`
    3. Update `trackExportPng` to remove `mode` parameter. Change signature to `trackExportPng(hasCustomTitle: boolean)`. Remove `mode` from the addAction payload.
    4. Remove any `'basic' | 'advanced'` type literals from analytics module

    **F. Rewrite `src/stores/wizardStore.test.ts`:**
    Remove the entire `mode switching` describe block and all tests that reference `setMode`, mode backup, or `'basic'`/`'advanced'`. Rewrite remaining tests to use the new flat `inputs` structure and `setInput` action. The test file should contain:
    - `describe('inputs')`: Test setting inputs via `setInput`, verify all fields work
    - `describe('prior shape inputs')`: Test setting priorShape, studentTDf (adapted from existing advanced inputs tests, no mode switching)
    - `describe('section navigation')`: Keep existing tests unchanged (they don't reference mode)
    - `describe('reset')`: Update to use `setInput` instead of `setSharedInput`/`setAdvancedInput`, remove mode assertions, remove sessionStorage backup assertions
  </action>
  <verify>
    <automated>ls src/components/welcome/ModeCard.tsx src/components/wizard/ModeToggle.tsx 2>&1 | grep -c "No such file" | grep -q "2" && echo "ModeCard and ModeToggle deleted" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/wizard.ts` does NOT contain `type Mode`
    - `src/types/wizard.ts` does NOT contain `interface SharedInputs` (merged into WizardInputs)
    - `src/types/wizard.ts` does NOT contain `interface AdvancedInputs` (merged into WizardInputs)
    - `src/types/wizard.ts` does NOT contain `interface InputsState`
    - `src/types/wizard.ts` DOES contain `interface WizardInputs`
    - `src/types/wizard.ts` DOES contain `export const initialInputs: WizardInputs`
    - `src/stores/wizardStore.ts` does NOT contain `setMode`
    - `src/stores/wizardStore.ts` does NOT contain `sessionStorage.setItem`
    - `src/stores/wizardStore.ts` does NOT contain `sessionStorage.getItem`
    - `src/stores/wizardStore.ts` does NOT contain `mode:`
    - `src/stores/wizardStore.ts` DOES contain `setInput:`
    - `src/lib/analytics.ts` does NOT contain `trackModeSelected`
    - `src/lib/analytics.ts` does NOT contain `'basic'`
    - File `src/components/welcome/ModeCard.tsx` does not exist
    - File `src/components/wizard/ModeToggle.tsx` does not exist
    - `src/stores/wizardStore.test.ts` does NOT contain `setMode`
  </acceptance_criteria>
  <done>Mode type removed, store simplified to single-input model, ModeCard and ModeToggle components deleted, analytics updated, store tests rewritten for single-mode architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Update all consumers to single-mode EVSI-only architecture and fix all tests</name>
  <files>
    src/pages/WelcomePage.tsx
    src/pages/CalculatorPage.tsx
    src/components/forms/UncertaintyPriorForm.tsx
    src/components/export/ExportButton.tsx
    src/components/export/ExportCard.tsx
    src/hooks/useExportPng.ts
    src/hooks/useEVSICalculations.ts
    src/hooks/useEVSICalculations.test.ts
    src/components/results/AdvancedResultsSection.tsx
    src/components/results/AdvancedResultsSection.test.tsx
    src/App.tsx
    src/App.test.tsx
  </files>
  <read_first>
    src/pages/WelcomePage.tsx
    src/pages/CalculatorPage.tsx
    src/components/forms/UncertaintyPriorForm.tsx
    src/components/export/ExportButton.tsx
    src/components/export/ExportCard.tsx
    src/hooks/useExportPng.ts
    src/hooks/useEVSICalculations.ts
    src/hooks/useEVSICalculations.test.ts
    src/components/results/AdvancedResultsSection.tsx
    src/components/results/AdvancedResultsSection.test.tsx
    src/App.tsx
    src/App.test.tsx
    src/components/forms/BaselineMetricsForm.tsx
    src/components/forms/ThresholdScenarioForm.tsx
    src/components/forms/ExperimentDesignForm.tsx
    src/stores/wizardStore.ts
    src/types/wizard.ts
  </read_first>
  <action>
    **A. Update `src/pages/WelcomePage.tsx` (DEPR-01):**
    1. Remove import of `ModeSelection` from `@/components/welcome/ModeCard`
    2. Remove import of `useWizardStore`
    3. Remove `const { mode, setMode } = useWizardStore()` line
    4. Remove the entire `{/* Mode Selection */}` div block (the `<ModeSelection>` component)
    5. Keep the hero section (title, description), CTA button, and footer intact
    6. The page now shows: title, description, "Get Started" button, footer -- no mode selection

    **B. Update `src/pages/CalculatorPage.tsx` (DEPR-03):**
    1. Remove import of `ModeToggle` from `@/components/wizard/ModeToggle`
    2. Remove import of `ResultsSection` from `@/components/results` (it no longer exists)
    3. Remove `const mode = useWizardStore((state) => state.mode)` line
    4. Remove `BASIC_SECTIONS` constant entirely
    5. Rename `ADVANCED_SECTIONS` to `SECTIONS` -- this is now the single section list:
       ```typescript
       const SECTIONS: SectionConfig[] = [
         { id: 'baseline', label: 'Baseline', title: 'Baseline Metrics' },
         { id: 'uncertainty', label: 'Uncertainty', title: 'Uncertainty (Prior)' },
         { id: 'threshold', label: 'Threshold', title: 'Shipping Threshold' },
         { id: 'test-design', label: 'Test Design', title: 'Experiment Design' },
         { id: 'results', label: 'Results', title: 'Results' },
       ];
       ```
    6. Remove the `useMemo` wrapping sections selection. Replace `const sections = useMemo(...)` with `const sections = SECTIONS`
    7. Remove `<ModeToggle />` from the header JSX (line 298)
    8. In the results section rendering, remove the dual conditional:
       - Remove: `{section.id === 'results' && mode === 'basic' && (<ResultsSection ... />)}`
       - Change: `{section.id === 'results' && mode === 'advanced' && (<AdvancedResultsSection />)}`
       - To: `{section.id === 'results' && <AdvancedResultsSection />}`
    9. In the footer, remove the `{mode === 'basic' && (...Hubbard attribution...)}` conditional block entirely

    **C. Update `src/components/forms/UncertaintyPriorForm.tsx`:**
    1. Find all `mode === 'basic'` conditionals. There are two blocks gated by this:
       - The "Fill with Recommended Default" button (line ~369-395): This should now always show. Remove the `{mode === 'basic' && (` wrapper, keep the button content.
       - The "Or specify your own 90% credible interval" label (line ~400-407): This should now always show. Remove the `{mode === 'basic' && (` wrapper, keep the label content.
    2. Update any `useWizardStore` selectors that read `mode` -- remove those selectors
    3. Update any `setSharedInput` calls to `setInput` and any `setAdvancedInput` calls to `setInput`
    4. If the form reads `state.inputs.shared.X` or `state.inputs.advanced.X`, update to `state.inputs.X` (flat structure)

    **D. Update `src/hooks/useEVSICalculations.ts`:**
    1. Remove the mode check that returns null for basic mode. The hook should only check if inputs are complete.
    2. Update store selectors from `state.inputs.shared.X` and `state.inputs.advanced.X` to `state.inputs.X`
    3. Update any references to `setSharedInput`/`setAdvancedInput` to `setInput`

    **E. Update `src/hooks/useEVSICalculations.test.ts`:**
    1. Remove any test that sets `mode: 'basic'` to verify null return
    2. Update store state setup to use flat `inputs` structure instead of `inputs.shared` / `inputs.advanced` nesting
    3. Update `setSharedInput`/`setAdvancedInput` calls to `setInput`

    **F. Update `src/components/export/ExportButton.tsx`:**
    1. Remove the `BasicModeProps` interface entirely
    2. Remove the `mode` discriminated union. The component now only handles EVSI/Advanced mode exports.
    3. Simplify `ExportButtonProps` to be a single interface (not a union):
       ```typescript
       interface ExportButtonProps {
         evsiResults: EVSICalculationResults;
         sharedInputs: SharedInputs;
         prior: PriorDistribution;
         testDurationDays?: number;
       }
       ```
    4. Remove all `mode === 'basic'` branches in the component body
    5. Remove `import type { EVPIResults } from '@/lib/calculations/types'`
    6. The `exportPng` call no longer needs a mode parameter -- update to just `exportPng(customTitle || undefined)`
    7. The ExportCard render no longer needs `mode` prop -- pass `mode="advanced"` hardcoded (or remove mode concept from ExportCard entirely in step G)

    **G. Update `src/components/export/ExportCard.tsx`:**
    1. Remove `mode` prop from the interface
    2. Remove the `modeBadge` variable that renders "Basic Mode" or "Advanced Mode"
    3. Remove all `mode === 'basic'` conditional branches in the JSX
    4. Keep only the advanced/EVSI rendering paths (EVSI value, net value, CoD breakdown)
    5. If removing mode entirely would be too disruptive, just hardcode mode behavior to always be "advanced"

    **H. Update `src/hooks/useExportPng.ts`:**
    1. Remove `mode` parameter from the `exportPng` function signature. New signature: `exportPng: (customTitle?: string) => Promise<void>`
    2. Update `generateFilename` to remove mode parameter. Always use the "advanced" filename pattern: `should-i-test-that-{sanitized-title}.png` or `should-i-test-that-analysis.png` (drop the "evpi-ceiling"/"evsi-analysis" suffix distinction)
    3. Update `trackExportPng` call to not pass mode

    **I. Update `src/components/results/AdvancedResultsSection.tsx`:**
    1. If it reads `mode` from the store, remove that selector
    2. This component should already work without mode -- verify it doesn't reference mode anywhere
    3. Rename the component from `AdvancedResultsSection` to `ResultsSection` throughout:
       - Update the function name: `export function ResultsSection()`
       - Update the file header comment
       - Update the export in `src/components/results/index.ts` to: `export { ResultsSection } from './AdvancedResultsSection';` (keep file name for now, just update export name)

    **J. Update `src/components/results/AdvancedResultsSection.test.tsx`:**
    1. Update any mode references in test setup
    2. Update component import name if renamed

    **K. Update `src/App.test.tsx`:**
    1. Remove the test `'renders mode selection cards'` that checks for "Basic Mode" and "Advanced Mode" text
    2. Remove `'Calculator mode'` aria-label assertion from the calculator navigation test (ModeToggle is gone)
    3. Update remaining tests to work without mode selection UI

    **L. Update `src/App.tsx`:**
    1. No structural changes needed -- it already routes between WelcomePage and CalculatorPage
    2. Verify no mode imports

    **M. Update ALL form components** that use `setSharedInput` or `setAdvancedInput`:
    Check these files and update store selectors:
    - `src/components/forms/BaselineMetricsForm.tsx`: `state.inputs.shared.X` -> `state.inputs.X`, `setSharedInput` -> `setInput`
    - `src/components/forms/ThresholdScenarioForm.tsx`: same pattern
    - `src/components/forms/ExperimentDesignForm.tsx`: `state.inputs.advanced.X` -> `state.inputs.X`, `setAdvancedInput` -> `setInput`
    - `src/components/forms/PriorShapeForm.tsx`: same pattern as ExperimentDesign
    - `src/components/forms/inputs/PercentageInput.tsx`, `NumberInput.tsx`, `CurrencyInput.tsx`: check if they reference mode

    **N. Run full test suite:**
    `npx vitest --run --reporter=verbose`
    ALL tests must pass. Fix any remaining failures.
  </action>
  <verify>
    <automated>npx vitest --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest --run` exits with 0 (all tests pass)
    - `grep -r "mode.*basic\|basic.*mode\|'basic'\|\"basic\"" src/ --include="*.ts" --include="*.tsx"` returns NO matches
    - `grep -r "ModeToggle\|ModeCard\|ModeSelection" src/ --include="*.ts" --include="*.tsx"` returns NO matches
    - `grep -r "setMode\b" src/ --include="*.ts" --include="*.tsx"` returns NO matches
    - `grep -r "setSharedInput\|setAdvancedInput" src/ --include="*.ts" --include="*.tsx"` returns NO matches
    - `grep -r "inputs\.shared\.\|inputs\.advanced\." src/ --include="*.ts" --include="*.tsx"` returns NO matches
    - `grep -r "wizard-advanced-backup\|wizard-basic-backup" src/ --include="*.ts" --include="*.tsx"` returns NO matches
    - `src/pages/WelcomePage.tsx` does NOT contain `ModeSelection`
    - `src/pages/CalculatorPage.tsx` does NOT contain `ModeToggle`
    - `src/pages/CalculatorPage.tsx` does NOT contain `BASIC_SECTIONS`
    - `src/pages/CalculatorPage.tsx` DOES contain `SECTIONS` (single section list with 5 entries)
    - `src/App.test.tsx` does NOT contain `Basic Mode`
    - `npx tsc --noEmit` exits with 0 (no type errors)
  </acceptance_criteria>
  <done>All consumer files updated to single-mode EVSI architecture. No trace of Basic/Advanced mode distinction remains. All tests pass. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx vitest --run` -- all tests pass
2. `npx tsc --noEmit` -- no type errors
3. `grep -rn "basic\|EVPI\|Mode.*basic\|ModeToggle\|ModeCard\|setMode\|setSharedInput\|setAdvancedInput\|inputs\.shared\|inputs\.advanced\|wizard-advanced-backup\|wizard-basic-backup" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v ".test."` should return zero matches (excluding any test files that might have residual comments)
4. The app renders: Welcome page (no mode selector) -> Calculator (5 sections, no mode toggle, EVSI results only)
</verification>

<success_criteria>
- DEPR-01: Welcome screen has no mode selector; calculator loads directly into single EVSI mode
- DEPR-03: All mode-switching UI, sessionStorage backup, and dual-mode conditional rendering gone
- No type `Mode = 'basic' | 'advanced'` anywhere in codebase
- No `setMode`, `setSharedInput`, `setAdvancedInput` in store
- Flat `WizardInputs` type replaces `SharedInputs` + `AdvancedInputs` + `InputsState`
- Single `SECTIONS` constant (5 sections) replaces `BASIC_SECTIONS` + `ADVANCED_SECTIONS`
- ModeToggle.tsx and ModeCard.tsx files deleted
- All tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/19-basic-mode-deprecation/19-02-SUMMARY.md`
</output>
