---
phase: 25.2-stats-engine-v3-audit-fixes
verified: 2026-04-10T16:32:00Z
status: human_needed
score: 10/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open calculator with Student-t prior selected, set interval to [-8.22, 8.22] and df=5. Observe the inline scale display."
    expected: "Scale shows approximately 4.08% (t-calibrated), NOT 5.00% (Normal sigma). Label reads 'scale' not 'sigma'. Hovering 'scale' shows tooltip: 'Student-t scale parameter controlling spread'."
    why_human: "UncertaintyPriorForm uses useMemo — can only verify the render output in a live browser, not statically."
  - test: "Observe the dailyTraffic field label on the Experiment Design section. Hover the derive-from-annual button."
    expected: "Field label reads 'Total daily traffic (before eligibility)'. Hovering the derive button shows tooltip 'Sets daily traffic = annual visitors / 365 (total traffic, before eligibility filtering)'."
    why_human: "Label and title attribute rendering must be confirmed visually; grep confirmed the strings exist in code."
  - test: "Set a wide Normal prior (e.g., interval [-40%, +40%]) with a high baseline CR (e.g., 80%). Run the calculator and inspect the Prior Belief card in results."
    expected: "When truncation is material (|effectivePriorMean - rawPriorMean| > 0.001), a line appears: 'After feasibility truncation: +X.X%' below the prior interval display."
    why_human: "Truncation disclosure depends on runtime effectivePriorMean from the hook; need a live browser session with that prior/CR combination."
  - test: "Inspect the ValueBreakdownCard in results for a case where timing costs are negative (test faster than status quo). Also check export PNG."
    expected: "Timing costs row labeled 'Timing costs (est.)'. When negative, shows '+$X' instead of '-$X'. Same label and sign handling appears in the exported image."
    why_human: "Sign-flip logic confirmed in code; need browser to confirm the conditional rendering produces the expected visual output."
---

# Phase 25.2: Stats Engine V3 Audit Fixes — Verification Report

**Phase Goal:** Remaining v3 audit findings resolved: effective-prior metrics are deterministic (no MC noise), impossible priors produce a clear warning, traffic-population semantics are documented, Student-t form displays t-calibrated scale, timing-costs sign is handled honestly, dead store field removed, and URL validation matches form rules
**Verified:** 2026-04-10T16:32:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | computeEffectivePriorMetrics is deterministic for Normal (analytic), Uniform (closed-form), and Student-t (numerical) priors | VERIFIED | evsi.ts lines 67-220: case 'normal' uses `truncatedNormalMeanTwoSided`, case 'uniform' uses closed-form clipped bounds, case 'student-t' uses 200-point composite Simpson's rule (201 nodes). No MC sampling loop. Tests pass: "returns identical results on successive calls (no MC noise)" |
| 2 | EVSI and net-value MC use identical effective-prior data from a single computation | VERIFIED | useEVSICalculations.ts line 256: `effectiveMetrics = computeEffectivePriorMetrics(...)` computed once. Lines 278, 317-318, 332-333: same `effectiveMetrics` passed to both `calculateNetValueMonteCarlo` and `calculateEVSIMonteCarlo`. |
| 3 | Zero-feasible-mass prior produces infeasible_prior_support warning (not silent untruncated fallback) | VERIFIED | types.ts: `'infeasible_prior_support'` in CalculationWarning code union. feasibility.ts: `checkInfeasiblePriorWarning` with docstring "NaN never reaches dollar outputs." evsi.ts and net-value.ts both return `evsiDollars: 0` / `netValueDollars: 0` with warning. Test: "returns NaN pair for Uniform[5,10] with CR0=0.5" and "returns evsiDollars=0 (NOT NaN)" both pass. |
| 4 | dailyTraffic label says "Total daily traffic (before eligibility)" with documented rollout assumption | VERIFIED | ExperimentDesignForm.tsx line 223: `label="Total daily traffic (before eligibility)"`. Line 234: derive button `title="Sets daily traffic = annual visitors / 365 (total traffic, before eligibility filtering)"`. BaselineMetricsForm.tsx line 175: "Eligibility affects test precision only; rollout value applies to all annual visitors." |
| 5 | Student-t form displays t-calibrated scale parameter labeled "scale" (not Normal sigma) | VERIFIED | UncertaintyPriorForm.tsx lines 200-213: dispatches on `inputs.priorShape === 'student-t'` to `buildPriorFromInputs` (t-quantile calibration). Line 610: `<span title="Student-t scale parameter controlling spread">scale</span>`. No `computePriorFromInterval` call for student-t. |
| 6 | Timing costs row handles negative values honestly and is labeled "(est.)" | VERIFIED | ValueBreakdownCard.tsx line 82: "Timing costs (est.)". Line 91: `timingCostsDollars > 0 ? '-' : timingCostsDollars < 0 ? '+' : ''`. ExportCard.tsx line 521: "Timing costs (est.)". Line 533: same ternary for sign. |
| 7 | Results and export show "Value scaled to all annual visitors (assumes full rollout after test)" | VERIFIED | AdvancedResultsSection.tsx line 186: text present. ExportCard.tsx line 543: text present. |
| 8 | sharedNetValue dead store field is removed | VERIFIED | `grep -r sharedNetValue src/` finds zero matches. Confirmed absent from wizardStore.ts, types/wizard.ts, AdvancedResultsSection.tsx, EVSIVerdictCard.tsx. |
| 9 | URL decoding rejects CR=0, CR=1, trafficSplit outside [10%, 90%] | VERIFIED | url-codec.ts line 77: `SCHEMA_VERSION = 2`. Lines 221, 234: `if (version >= 2)` gates on strict CR interval `(0, 1)` and trafficSplit range `[0.10, 0.90]`. v1 backward compat preserved. All 8 new url-codec.test.ts tests pass. |
| 10 | Sample sizes are real-valued in calculation layer (Math.floor removed) | VERIFIED | sample-size.ts line 14: "Results are real-valued for smooth calculation behavior. Round to integers only for display." Line 86: "Real-valued: no Math.floor". `grep Math.floor sample-size.ts` finds zero matches. |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/calculations/evsi.ts` | Deterministic computeEffectivePriorMetrics | VERIFIED | All three prior cases (normal/uniform/student-t) use analytic/numerical methods |
| `src/lib/calculations/types.ts` | infeasible_prior_support in CalculationWarning | VERIFIED | Line 191: code union includes `'infeasible_prior_support'` |
| `src/lib/calculations/sample-size.ts` | Real-valued sample size derivation | VERIFIED | No Math.floor; comment says "real-valued" |
| `src/hooks/useEVSICalculations.ts` | effectivePriorMean in EVSICalculationResults | VERIFIED | Line 55: `effectivePriorMean: number` in interface; lines 387-410: computed and returned |
| `src/components/forms/ExperimentDesignForm.tsx` | Updated dailyTraffic label and derive tooltip | VERIFIED | "Total daily traffic (before eligibility)" present; derive button title present; stale comment absent |
| `src/components/forms/UncertaintyPriorForm.tsx` | Student-t scale display via buildPriorFromInputs | VERIFIED | buildPriorFromInputs used; "scale" label with tooltip |
| `src/lib/prior.ts` | Test verifying Student-t scale differs from Normal sigma | VERIFIED (test in prior.test.ts) | 2 new tests in prior.test.ts confirming t-calibration |
| `src/components/results/AdvancedResultsSection.tsx` | TRUNCATION_DISPLAY_THRESHOLD constant, NaN guard, assumption note | VERIFIED | Line 41: constant; lines 131-137: NaN guard; line 186: assumption note |
| `src/components/results/ValueBreakdownCard.tsx` | Honest timing-costs sign, estimate label | VERIFIED | "Timing costs (est.)" + ternary sign logic |
| `src/components/export/ExportCard.tsx` | Assumption note in export, timing-costs sign fix | VERIFIED | Both "Value scaled to all annual visitors" and sign-honest timing costs |
| `src/stores/wizardStore.ts` | sharedNetValue removed | VERIFIED | Zero occurrences across src/ |
| `src/lib/url-codec.ts` | Versioned validation v2+ tightened, v1 legacy preserved | VERIFIED | SCHEMA_VERSION=2; version-aware validation; v1 compat comment |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| src/lib/calculations/evsi.ts | src/lib/calculations/net-value.ts | Shared effectivePriorMetrics computed once by caller | VERIFIED | useEVSICalculations.ts computes once, passes to both functions; net-value.ts accepts via default parameter |
| src/hooks/useEVSICalculations.ts | src/lib/calculations/evsi.ts | computeEffectivePriorMetrics called once, result reused | VERIFIED | Line 28: import confirmed; lines 256-260: single computation; effectivePriorMean surfaced in results |
| src/components/results/AdvancedResultsSection.tsx | src/hooks/useEVSICalculations.ts | results.effectivePriorMean from EVSICalculationResults with NaN fallback | VERIFIED | Line 134: `!isNaN(results.effectivePriorMean)` NaN guard; line 320: passes to ExportButton |
| src/lib/url-codec.ts | src/lib/validation.ts | Validation rules aligned for v2+; v1 uses original loose rules | VERIFIED | version-aware branching at lines 221 and 234; comment at line 144 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| AdvancedResultsSection.tsx | effectivePriorMean | useEVSICalculations hook → computeEffectivePriorMetrics | Yes — deterministic computation from prior params | FLOWING |
| ValueBreakdownCard.tsx | timingCostsDollars | Parent-passed prop from netResults.netValueDollars and evsiResults.evsiDollars | Yes — MC simulation results | FLOWING |
| ExportCard.tsx | effectivePriorMean | Prop passed from AdvancedResultsSection → ExportButton → ExportCard | Yes — NaN-guarded at ExportCard line 144 | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All engine calculation tests pass | npx vitest run src/lib/calculations/ | 155/155 tests pass | PASS |
| Hook tests pass (effectivePriorMean in results) | npx vitest run src/hooks/ | 19/19 tests pass | PASS |
| URL codec v2 validation tests pass | npx vitest run src/lib/url-codec.test.ts | 6 new v2 tests, 2 backward compat tests all pass | PASS |
| TRUNCATION_DISPLAY_THRESHOLD boundary tests pass | npx vitest run src/components/results/ | 2 boundary tests pass | PASS |
| Full test suite | npx vitest run | 571/571 tests pass across 26 test files | PASS |

### Requirements Coverage

The plan requirement IDs (Audit-1, Audit-3, Audit-4, Audit-7, Audit-8, D-01, D-02, D-03, D-04, Other-1, Other-2, Other-3, Simplification-4) are phase-specific audit tracking IDs defined in `25.2-CONTEXT.md` and the v3 audit document. They do not appear in REQUIREMENTS.md, which tracks formal product requirements (ENG-*, GUIDE-*, etc.). This is intentional — phase 25.2 is an audit remediation phase with its own ID namespace.

All 13 audit tracking IDs are accounted for across the three plans:

| Audit ID | Plan | Description | Status |
|----------|------|-------------|--------|
| Audit-1 | 25.2-01 | Deterministic effective-prior metrics | SATISFIED |
| Audit-3 | 25.2-03 | Truncated-prior reporting text uses effectivePriorMean | SATISFIED |
| Audit-4 | 25.2-02 | Student-t form displays t-calibrated scale | SATISFIED |
| Audit-7 | 25.2-01 | Single MC computation for EVSI and net-value | SATISFIED |
| Audit-8 | 25.2-03 | Timing-costs sign handled honestly | SATISFIED |
| D-01 | 25.2-02 | annualVisitors tooltip clarifies rollout population | SATISFIED |
| D-02 | 25.2-02 | Derive button tooltip says "total traffic, before eligibility filtering" | SATISFIED |
| D-03 | 25.2-02 | dailyTraffic label renamed to "Total daily traffic (before eligibility)" | SATISFIED |
| D-04 | 25.2-03 | Assumption note in results/export: "Value scaled to all annual visitors" | SATISFIED |
| Other-1 | 25.2-03 | sharedNetValue dead field removed | SATISFIED |
| Other-2 | 25.2-03 | URL validation tightened to match form rules | SATISFIED |
| Other-3 | 25.2-01, 25.2-03 | Stale comments corrected (net-value.ts, AdvancedResultsSection.tsx, ExperimentDesignForm.tsx) | SATISFIED |
| Simplification-4 | 25.2-01 | Real-valued sample sizes (Math.floor removed) | SATISFIED |

No orphaned requirements in REQUIREMENTS.md — the formal product requirements for phase 25.2 are captured under the roadmap goal rather than as numbered REQUIREMENTS.md entries.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| evsi.ts line 525, net-value.ts line 211 | effectivePriorMetrics has default parameter value rather than being TypeScript-required | Info | Documented deviation: plan acceptance criteria said "no `?`" (truly required), but SUMMARY explains default parameter chosen for backward compatibility. Goal achieved in practice: the hook always passes it explicitly. Not a stub — the default computes correctly. |

No blockers or warnings found. The one info-level note is a documented deviation that does not affect correctness.

### Human Verification Required

#### 1. Student-t Scale Parameter Display

**Test:** Open the calculator, select the "Student-t" prior shape. Set the interval to [-8.22%, 8.22%] and degrees of freedom to 5. Look at the inline parameter annotation below the interval sliders.
**Expected:** The annotation shows approximately "scale: 4.08%" (not 5.00%). The label "scale" is present (not the Greek letter sigma). Hovering "scale" shows tooltip: "Student-t scale parameter controlling spread".
**Why human:** The useMemo dispatch logic is verified in code, but the rendered output in the form requires a live browser session.

#### 2. Traffic Field Label and Derive Button Tooltip

**Test:** Open the calculator, navigate to the Experiment Design section. Observe the label on the daily traffic input field. Hover over the "(derive: X/day)" button next to annual visitors.
**Expected:** Field label reads "Total daily traffic (before eligibility)". Button hover tooltip says "Sets daily traffic = annual visitors / 365 (total traffic, before eligibility filtering)".
**Why human:** Label and title attribute confirmed present in code; must visually confirm they render correctly and the tooltip appears on hover.

#### 3. Truncation Disclosure Display

**Test:** Enter a wide Normal prior (e.g., interval [-40%, +40%]) with a high baseline conversion rate (80%). Run calculations. Inspect the Prior Belief card in the results section.
**Expected:** When the effective prior mean after feasibility truncation differs materially from the raw midpoint (0% for this symmetric prior), a line appears below the prior display: "After feasibility truncation: +X.X%". The exact value depends on liftFeasibilityBounds for CR0=0.80.
**Why human:** Requires a specific combination of inputs and a live calculation to trigger the truncation disclosure. The TRUNCATION_DISPLAY_THRESHOLD boundary is verified by automated tests, but the visual disclosure must be confirmed in-browser.

#### 4. Timing Costs Sign in Results and Export

**Test:** Create a scenario where testing delays a beneficial rollout (e.g., short duration, large prior mean). Inspect the ValueBreakdownCard. Export the results as a PNG.
**Expected:** The timing costs row shows "Timing costs (est.)" as the label. If timing costs are negative (rare but possible from MC noise), the sign shows "+" not "-". The export PNG shows the same "(est.)" label.
**Why human:** The sign-flip conditional is verified in code, but the negative-sign case requires the right MC outcome to observe. Export PNG rendering requires a live browser session.

### Gaps Summary

No gaps found. All 10 roadmap success criteria are verified in the codebase. The four items above require human confirmation for UI rendering correctness; all underlying logic and data flows are verified as WIRED and FLOWING.

---

_Verified: 2026-04-10T16:32:00Z_
_Verifier: Claude (gsd-verifier)_
